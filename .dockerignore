*.o
*.a
.cache/
# Do not ignore .git directory, otherwise the reported build number will always be 0
.github/
.gitignore
.vs/
.vscode/
.DS_Store

build*/

# worker tensor-cache volume from running a local rpc-worker compose in this
# tree - tens of GB that must never enter the build context
cache-rpc/

tools/ui/node_modules/

models/*

/llama-cli
/llama-quantize

arm_neon.h
compile_commands.json
Dockerfile
