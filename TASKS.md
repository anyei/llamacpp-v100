# Token-Generation Improvement Round — Task Tracker

Goal (from `do_your_best.md`): best possible generation quality (no regression), faster prefill/prompt caching, and above all faster token generation on 2x (future 4x) Tesla V100. Baselines and setup details: see memory `v100-inference-setup.md` and commit `d2b706d1b`.

Status legend: ✅ done · 🔄 in progress · ⬜ pending

## ✅ Done (verified, measured)

| # | Task | Where | Result |
|---|------|-------|--------|
| 1 | MTP draft padding to fixed `n_max` so batch shape stays stable and graph reuse works | `common/speculative.cpp` | Single-stream spec 33.6 → 62–65 t/s. Kill-switch: `LLAMA_SPEC_DRAFT_NO_PAD` |
| 2 | Make `llama_meta_device_get_split_state` regexes `static const` (were rebuilt ~29x per tensor per alloc) | `src/llama-model.cpp` | Graph alloc 38ms → 4–6ms; 4-conc spec aggregate 33 → 61 t/s |
| 3 | `GGML_CUDA_FORCE_GRAPHS` env override for the Volta (cc<8.0) CUDA-graphs gate | `ggml-cuda.cu` | Tested: no gain (launch overhead not the bottleneck). Kept as opt-in |
| 4 | One-shot P2P NVLink AllReduce for 2 GPUs (`GGML_CUDA_ALLREDUCE=p2p`, NCCL fallback, 4MB cap via `GGML_CUDA_AR_P2P_MAX_BYTES`) | `ggml-cuda/allreduce.cu(+cuh)`, `ggml-cuda.cu` | tg 48.8 → 49.4 (+1.2%), ppl identical. Committed `d2b706d1b` |
| 5 | Env-gated instrumentation: `LLAMA_SPEC_TIMING` (server spec phases), `LLAMA_DECODE_TIMING` (build/alloc/set-inputs/compute per ubatch) | server, `src/llama-context.cpp` | Used to find items 1–2 and 8 |
| 6 | Distributed-inference analysis doc (coordinator + N workers over RPC) | `docs/distributed-inference-plan.md` | Doc only; implementation deferred (item 12) |
| 7 | Exactness verification | — | temp-0 output identical spec vs nospec |
| 8 | Multi-shape decode graph cache: LRU cache of small-batch decode graphs, each entry with its own scheduler; meta backend compute-container ring 2 → N slots with on-demand shadow recreation and per-entry split-state invalidation. Env: `LLAMA_DECODE_GRAPH_CACHE` (entries, default 4, 0 disables), `LLAMA_DECODE_GRAPH_CACHE_TOKENS` (max cached ubatch size, default 64), `GGML_META_MAX_GRAPHS` (ring slots, default 8) | `src/llama-context.{cpp,h}`, `ggml/src/ggml-backend-meta.cpp` | Reuse 61–64/256 → 248–256/256 ubatches (steady state 100%, build+alloc 0ms); 4-conc spec aggregate 61.7 → 64.7 t/s (+5%); single-stream spec 66 t/s and nospec 48 t/s unchanged; temp-0 output identical cache on/off, spec and nospec. All output-extraction sites (logits/embd/nextn/sampling/layer-inputs) wired to `active_sched()`; `synchronize()` needs no change since all schedulers share the same backend objects |

## ⬜ Pending (approved roadmap, not started)

- [x] **9. GPU sampling in tensor-split mode** — investigated and measured, **not implemented (no headroom)**. Two findings: (a) the server disables backend sampling whenever speculation is active (`tools/server/server-context.cpp:1848`), so it can never help the MTP prod config; (b) for nospec serving the entire CPU sampling path is free — 4-conc aggregate 89.6 t/s greedy vs 89.5 t/s with full top-k/top-p/min-p chain vs 89.7 control (within noise): logits transfer + CPU sampler fully overlap GPU compute. Implementing would require a new allgather subgraph-boundary type in the meta backend (vocab-split logits hit the `handle_per_row` assert) for a measured ~0% gain.
- [x] **10. KV cache compression (TurboQuant)** — resolved: **existing quantized KV already meets the goal; TurboQuant not worth implementing.** Findings (Qwen3.6-27B, tensor mode, wikitext-2):
  - Quantized KV **works in tensor-split mode** (old note saying otherwise was stale), and this tree already ships upstream PR #21038 (commit `744c0c731`): Hadamard-rotated KV quantization — the same core trick as TurboQuant/QuaRot — auto-enabled whenever KV is quantized (opt-out `LLAMA_ATTN_ROT_DISABLE=1`).
  - PPL @32k ctx: f16 6.2904 · q8_0/q8_0 6.2922 (+0.03%, noise, **lossless, 2×**) · K q8_0/V q4_0 6.2993 (+0.14%, **2.67×**) · q4_0/q4_0 6.3093 (+0.30%, **4×**). @8k ctx all statistically identical. temp-0 128-tok gen byte-identical to f16 for q8_0 and q4_0. tg cost ≈ −3-4%.
  - Mixed K/V types crashed in tensor mode: default builds compile no FA kernel for K≠V (`fattn.cu:424`), the scheduler tries CPU fallback and the meta backend asserts (`ggml-backend-meta.cpp:557`). Fixed by building with `GGML_CUDA_FA_ALL_QUANTS=ON` (added to `.devops/cuda.Dockerfile`). Known residual: unsupported-op CPU fallback in tensor mode aborts instead of erroring cleanly.
  - TurboQuant itself (arXiv 2504.19874): no official code; needs a non-block ggml type (Lloyd-Max codebooks + per-token norms + QJL residual) and codebook gathers inside the V100 FA vec kernels; would buy ~3.5 effective bits vs q4_0+rotation's 4.5 (≈22% more capacity) — 2026 practice (SAW-INT4, vLLM) converged on rotated token-wise INT4, which is exactly what's already in-tree.
  - **Recommended prod setting: `-ctk q8_0 -ctv q8_0`** (strictly lossless, halves KV → doubles usable context/slots); `-ctk q8_0 -ctv q4_0` if more capacity is worth +0.14% ppl.
- [x] **11. Close the remaining 4-concurrency gap** — ✅ done (`abf23cdd1` + `65286b53f`). The open temp-0 drift was resolved by the simplify pass: the duplicate per-entry `graph_params` construction in `process_ubatch` was the culprit; with params built once and only the `res` pointer patched, fresh-server and cached-prompt np1 outputs both match the pre-split baseline byte-for-byte, and 4-conc spec holds at 125.8 t/s. Root cause: hybrid models (Qwen3.6 = attention + recurrent DeltaNet) force per-seq ubatch splits when spec rollback is on (`[TAG_RECURRENT_ROLLBACK_SPLITS]`), so the 4-slot verify ran as 4×35ms single-seq decodes instead of one 16-token ubatch (74.5ms; decode-scaling curve: 1 tok=20.8ms, 4=40.4, 8=69.6, 12=65.8 (MMVQ→MMQ crossover at 8), 16=74.5, 32=123).
  Fix: `split_equal(n_ubatch, sequential, full_seqs=true)` in `llama-batch.{h,cpp}` — groups consecutive equal-count seqs whole so no seq spans ubatches (preserves the rollback-snapshot invariant in `delta-net-base.cpp`); used by the rollback branches in `llama-memory-recurrent.cpp`, `llama-memory-hybrid.cpp`, `llama-memory-hybrid-iswa.cpp`.
  **Measured: 4-conc spec aggregate 64.7 → 125.0 t/s (nospec is 89.6 — spec now wins by 40%).** Single-stream unchanged (65.8).
  Before commit: (a) root-cause a 1-token temp-0 drift at np1 on fresh server only (output coherent, deterministic per build, matches baseline exactly when prompt is cached → suspect checkpoint/prefill interaction; diff full `LLAMA_BATCH_DEBUG=2` dumps of fresh 256-token runs on both builds); (b) validate np4 spec temp-0 outputs are coherent vs nospec (multi-seq K-snapshot path in `ggml_gated_delta_net` newly exercised); (c) quick ppl run to confirm no quality regression.
- [x] **13. GLM-4.7-Flash / MLA tensor-mode support** — ✅ **RESOLVED** (gate lifted 2026-07-07; new model **GLM-4.7-Flash-UD** validated 2026-07-09). Tensor mode is **byte-identical / PPL-neutral**; the historical "corrupts after ~2 tokens" was **MoE-router amplification of ordinary split-reduction noise + a tail-reduce bug** (fixed `d534f0aba`), not a split-state bug — see the RESOLVED and long-context sub-bullets below. **Recommendation: run GLM single-GPU** — it's as fast as tensor mode for an A3B MoE, and MLA is so KV-efficient the new GLM-4.7-Flash-UD holds its **full 202K native context on one 32 GB V100** (~10 GB KV), so 2-GPU tensor adds nothing. Historical root cause (v1 design): `deepseek2` arch with MLA (`n_head_kv=1`, one shared 576-dim latent) — the split policy head-splits `cache_k_l*`, colliding with the mirrored latent at `handle_set_rows` (pre-existing crash). Implemented v1 (mirror the whole attention block for `is_mla()` models + mirrored-QKV case in `handle_flash_attn_ext`; MoE experts still split): no longer crashes, 79.8 t/s. Verified inert for non-MLA models (Qwen temp-0 byte-identical).
  **Groundwork 2026-07-07 (1-GPU, no 2-GPU window needed):**
  - **Meta-of-1 discriminator**: GLM with `-sm tensor` on ONE device (`LLAMA_TENSOR_MLA_WIP=1`, meta wraps a single GPU) is **byte-identical** to plain single-GPU (76.9 vs 78.4 t/s) → the v1 graph logic is self-consistent; corruption arises only from real n≥2 splits.
  - **Full split-policy audit** (agent, all file:line refs preserved in git log of this entry): policy itself is CLEAN — attention block, router, norms, shared experts, `cache_k_l*` all resolve MIRRORED (only `attn_output.weight` + `cache_k_l*` need the explicit MLA rules; the rest default-mirror); only routed experts split (gate_up AXIS_1 segmented {n_ff_exp,2} → silu AXIS_0 → down `mul_mat_id` PARTIAL). Key mechanism: sources always resolve with `assume_sync=true` (ggml-backend-meta.cpp ~932), so PARTIAL is visible only at the node itself and correctness rests entirely on AllReduce placement — silent by construction, no assert can fire.
  - **AllReduce placement verified correct for GLM** via new env `GGML_META_DEBUG_REDUCE=1` (prints `partial N -> boundary M`; placement logic is n-independent, so a 1-GPU run is authoritative): boundary lands on the final expert-sum `ffn_moe_out` ADD, BEFORE the `ggml_add(moe_out, ffn_shexp)` merge; the delayed window contains only sum-commuting ops (mul by mirrored weights, expert-view adds). Audit suspects #1 (shexp merged pre-reduce) and #2 (get_i_delayed mismatch on GLM's op order) are **cleared**.
  - **Remaining suspects (need n=2 to verify)**: (3) `handle_mul_mat` AXIS_1×MIRRORED collapses the segmented gate_up state to 1 segment (ggml-backend-meta.cpp ~684-690) — with the merged `ffn_gate_up_exps` ({n_ff_exp,2} segments), the downstream gate/up views (llama-graph.cpp:1981-1983) may map to the wrong physical rows on each device's slice; check whether Qwen A3B (working) uses merged gate_up or separate tensors to discriminate. (4) `handle_flash_attn_ext` mirrored branch — untested at n≥2 by construction. (5) mirrored `cache_k_l*` desync via the zero-slice compute-flag disable inside delayed windows ("corrupts after ~2 tokens" fits cache divergence compounding).
  - **No 2-GPU box needed anymore**: `LLAMA_META_DUP_DEVICE=2` (d534f0aba) duplicates the device list so ONE GPU runs a genuine n=2 split. Validated: Qwen dup=2 byte-identical to single-GPU; **GLM dup=2 reproduces the corruption deterministically** (identical wrong text across builds — real math bug, not a race; the reduce machinery itself is validated by Qwen passing).
  - **Fix landed along the way (d534f0aba)**: the AllReduce loop skipped the LAST subgraph of every graph — a graph ending in a PARTIAL node (sched split after a partial, or per-node debug execution) silently never reduced. Subgraphs now carry a planning-time reduce flag. Also: meta get_tensor learned PARTIAL reads (sum of slices), making `eval-callback` usable on meta backends.
  - **Corruption localized** (eval-callback elementwise diff, 1-GPU vs dup=2): first real divergence is hidden (non-corner) elements of the **layer-0 dense-FFN output/residual** — `norm-1` shifts 17.8% per-row (RMS over l_out differs) while every sampled corner of `ffn_gate/up/swiglu/ffn_out/l_out` matches. Trace artifacts to ignore: PARTIAL nodes read 2× post-fix (state says partial, data already reduced); pre-fix traces showed a fake l_out-0 divergence from the then-missing tail reduce.
  - **Alignment hypothesis (simple form) dead**: dense n_ff=10240 → cut 5120, 256-aligned; gate=q4_K, down=q6_K both blck 256 → identical granularity/cuts.
  - **RESOLVED (2026-07-07, pending physical 2-GPU confirmation)**: full-tensor dumps (`LLAMA_DEBUG_DUMP_DIR`/`LLAMA_DEBUG_DUMP_FILTER`, landed in common/debug.cpp) showed every layer-0 tensor matching to reduction-noise level — there is NO split-state bug in v1. The observed temp-0 text divergence is **MoE-router amplification of ordinary split-reduction noise** (near-tied experts flip; dense Qwen shows zero divergence because nothing discontinuous amplifies the noise). **Quality is unaffected: PPL 14.3574±0.69 single-GPU vs 14.3787±0.69 at n=2 (dup harness) — statistically identical.** The historical "corrupts after ~2 tokens" is best explained by the tail-reduce bug fixed in d534f0aba (MoE routing's sched splits can end a graph on an unreduced PARTIAL — real corruption, silent, exactly matched the symptom). **Physical 2-GPU confirmation PASSED** (same day): Qwen 2-GPU byte-identical to reference (gates the meta fixes on real hardware); GLM 2-GPU coherent (30.6 t/s under heavy host-GPU contention — not a perf number). **Gate lifted**: the model-create rejection is now a WARN (attention mirrored, single GPU usually as fast); `LLAMA_TENSOR_MLA_WIP` env removed. Residual minor: a CUDA OOM during meta buffer alloc surfaces as `GGML_ASSERT(meta_buf_ctx->bufs[i])` instead of a clean error — make alloc failure propagate. 27B in-process gate for the day's meta changes still queued for a quiet 2-GPU window (n=2 exactness already held by Qwen dup=2 AND Qwen physical 2-GPU).
  - **New model + long-context capacity study (2026-07-09, `GLM-4.7-Flash-UD-Q4_K_XL`, 17.5 GB, deepseek2/MLA, 47 layers, `kv_lora_rank=512`, native `n_ctx_train=202752`)**: replaces the disliked REAP GLM; same arch. Tensor mode works (2-GPU `-sm tensor -ts 0.5,0.5` byte-identical to single-GPU). **Capacity conclusion — 2-GPU tensor buys NOTHING for this model**: MLA KV is only **~10.2 GB for the *full* 202,752-token native context** (measured VRAM delta: `-c 4096` → 17.2 GB peak, `-c 202752` → 27.6 GB peak, both 1 GPU), so **one 32 GB V100 holds GLM's entire native context with ~5 GB headroom** at ~89 t/s. Measured ceilings (1 GPU): `-c 131072` fits (94 t/s), `-c 202752` fits (94 t/s). 2-GPU tensor at full context also fits but is **slower (70 t/s)** — AllReduce overhead, no compute or capacity benefit. **Recommended GLM config = single-GPU, `-c 202752`** (or any context up to native). Tensor mode only helps a model whose weights+KV exceed one card; MLA makes GLM not that model. Optional: q8_0 KV would halve the ~10 GB KV → ~22 GB total at full context, more headroom still.
- [x] **14. Meta-device model teardown segfault** — ✅ resolved (2026-07-07), and the original diagnosis was wrong: `llama_model_free` on a meta-device model without a context is SAFE (verified under gdb with a breakpoint on the free — clean exit; likely fixed incidentally by the shadow/fingerprint refactors, or never broken). The actual segfault behind "context-creation failure crashes" was the **server dereferencing the null context** (`llama_n_ctx(nullptr)` in `load_model` — reproduced with `-sm tensor -fa off`, backtrace lands in load_model, not in any free). Fixes: (a) server checks `ctx == nullptr` after init; (b) systemic: `common_init_from_params` now DROPS the model when context creation (or lora load) fails, so the four other tools that only check `model()==nullptr` (results, cvector-generator, mtmd-cli, perplexity) are covered without touching them; (c) bonus: fixed a pre-existing **double-free** in the lora-failure path (`pimpl->model.reset(model)` re-stored the pointer the unique_ptr already owned). Verified: `-fa off` in tensor mode now exits with a clean error; positive path (`-fa on`) byte-identical to reference. The MLA gate stays at model create — erroring before the weights load is better UX anyway. Footnote: teardown verified with a 1-GPU meta device (GPU0 occupied); re-run the `-fa off` repro with both GPUs free for completeness.
- [x] **23. SSD-streaming correctness hardening** — ✅ **DONE (2026-07-10, `37e21180a`) with one recorded residual.** (a) same-pass eviction pinning (pass epochs; overshoot+warn instead of zeroed experts — the MRU-front insight makes the pinned-tail check O(1)); (b) pread failures abort loudly (serial + parallel); (c) eval-callback displacement warns (`llama-context.cpp`); (d) defensive H2D sync before pins release. **Gates: PPL byte-equal at 24 GB / 1 GB / 512 MB budgets (7.0898 / 6.2022 / 5.1397); overshoot warning fires at 48 MB.** **RESIDUAL (open):** at ~48 MB — less than ONE node's expert union, ~100× below practical budgets — output is deterministic but slightly divergent (chunk-1 PPL 4.0573 vs 4.0331). NOT a race (identical across two builds, sync made no difference; my async-DMA hypothesis disproven) — a systematic path difference at absurd budgets, unisolated. Practical impact nil; revisit if tiny-budget configs ever matter. Original finding text: Three verified silent-corruption hazards, none yet observed in practice but all reachable: (a) **same-pass eviction** — within one `gpu_bind`/prefill pass, a slice that was a RAM *hit* early in the loop can be evicted (`MADV_DONTNEED`) by a later insertion in the same pass when the node's expert working set exceeds `LLAMA_SSD_STREAM_BUDGET`; phase-3 H2D (or `copy_experts`) then reads zeroed pages — **silent garbage** instead of the documented "budget must cover one ubatch's union" caveat failing loudly. Fix: pin this-pass slices against eviction (or force-read every VRAM miss in phase 2); minimum: detect + abort. (b) **pread failures swallowed** — `ssd_pread_full`'s bool is ignored and the parallel workers discard returns; a slice is inserted as resident *before* its deferred read runs, so a transient disk error poisons the cache permanently. Fix: propagate failure, evict the slice from the index, abort loudly. (c) **eval-callback conflict** (`llama-context.cpp:1432`) — the sched has ONE eval-cb slot; any `cb_eval` (e.g. the `LLAMA_DEBUG` dump cb) displaces the SSD fill cb → streamed CPU-path experts never fill → silent garbage. Fix: warn at minimum; ideally chain (wrapper cb runs the fill then forwards).
- [x] **24. SSD-streaming lifecycle + robustness** — ✅ **DONE (2026-07-10) except (d), deliberately deferred.** (a) `ssd_stream_reset()` — full teardown (VRAM pools, ids scratches, dup fds, registry, SLRU) hooked into the LAST arena buffer's free (refcounted; fires on `llama_model_free`, i.e. the server model-swap path that used to leak VRAM+fds and risk stale-madvise into a reused arena). Note: cli/perplexity leak-at-exit by design and never free model buffers, so the reset doesn't log there — verified the code ships (string in libggml-base) and the mechanism is the buffer-free path itself. (b) **pool-OOM halving fallback**: `ensure_pool` halves K on alloc failure instead of leaving bind to first-token-abort — gate: `VRAM_BUDGET=40960` on a 32 GB card = 3 halvings, runs at 5.4 t/s, zero aborts (was: death). (c) **backend-type gate**: shrink + ssd_gpu now also require the split backend to BE a GPU device (no more "VRAM pool" on the CPU backend when op_offload is off). Regression: PPL 7.0898 unchanged. **(d) mutex narrowing — DEFERRED**: releasing `g_state.mutex` across phase-2/3 interacts with the pass-pinning epochs (another context's pass increment would unpin in-flight slices → reintroduces the #23 corruption); needs per-pass pin ownership, only matters for multi-model-per-process serving. Original finding text: (a) **No teardown**: `g_gpu_pools` (VRAM buffers), `fd_dup`, registry, and the SLRU state are process-global and never reset — server model-swap leaks VRAM+fds, and the stale SLRU can `madvise(MADV_DONTNEED)` into a *reused* arena address range of the NEXT model (cross-model corruption). Add `ggml_ssd_stream_reset()` called from model unload. (b) **First-token `GGML_ABORT` on pool OOM**: the reclaim shrinks the copy at reserve time, so a too-big `VRAM_BUDGET` kills the process at the first decode token instead of degrading — un-shrink + CPU-fallback retry, or validate the pool before committing the shrink. (c) **Gate on the actual backend type**: shrink/bind gate on `n_gpu==1` but never check the split backend IS the GPU — with op_offload off, a CPU-scheduled streamed matmul gets a "VRAM pool" allocated on the CPU backend (wrong tier, silent). (d) **Mutex scope**: `g_state.mutex` is held across the phase-2 disk reads and phase-3 H2D issue — two models in one process serialize on each other's I/O; shrink the critical section to the SLRU/pool bookkeeping.
- [x] **25. Minor polish (review leftovers)** — ✅ **DONE (2026-07-10).** All five: `n_gpu` cached on the sched (was recomputed ~141×/token); dlsym result no longer null-cached in `ggml_ssd_stream_buft`; portable `common_set_env` shim for the `--ssd-*` flags (MSVC `_putenv_s`); `pad_drafts` comment scoped to the MTP impl; `LLAMA_DEBUG_DUMP_DIR` paths sanitize `/` in tensor names + warn on fopen failure. Gates: PPL 5.1397 unchanged, CLI flags engage through the shim. Original list: Cache `ggml_backend_sched_n_gpu` on the sched (recomputed ~141×/token on the decode hot path); don't cache a null `dlsym` result in `ggml_ssd_stream_buft` (permanent null device if resolved too early under `GGML_BACKEND_DL`); portable `setenv` shim for the `--ssd-*` flags in `common/arg.cpp` (MSVC has no `setenv`); fix the `pad_drafts` comment in `common/speculative.cpp:156` (padding exists only in the MTP impl, not draft_simple/eagle3); sanitize `/` in tensor names for `LLAMA_DEBUG_DUMP_DIR` paths + warn on `fopen` failure.
- [x] **26. Distributed: worker-local model sourcing (skip the first-load network stream)** — ✅ **DONE (2026-07-10); real-fleet timing measurement pending.** Implemented exactly as designed, no protocol change: `rpc-server -md/--model-dir <dir>` — `rpc_server::build_local_index()` walks the dir recursively (matches `.gguf` anywhere in the name — the `?download=true` files count), parses each GGUF via the in-tree gguf API, and fnv-hashes every tensor **> HASH_THRESHOLD (10 MiB)** — only those go through the coordinator's `SET_TENSOR_HASH` path — into `hash → (file, offset, size)`. Index persists as a text sidecar in the cache dir (`modelidx-<fnv(path|mtime|size)>`), so restarts load instantly; no cache dir = re-hash each start (warned). Lookup: `set_tensor_hash` tries the weight cache, then `get_local_tensor()` — pread from the GGUF + **re-hash verification before serving** (a file changed after indexing falls back to streaming loudly). Coordinator untouched and unaware. Verified (loopback, CPU build): 0.6B — 311 MB embedding served from the local GGUF on a cold cache, generation byte-identical to a no-RPC reference; persisted index reloads instantly ("from cached index"); **mismatch gate**: worker indexed the 9B while the coordinator loaded the 0.6B → 0 local serves, clean streaming fallback, correct output. Known scope limits: (a) tensor-parallel islands stream slices, which never hash-match whole tensors — slice-aware indexing would be a follow-up; (b) tensors ≤ 10 MiB always stream (coordinator never hashes them). **Real-fleet validation DONE (2026-07-10 late evening):** all four workers ran `--model-dir` with the 35B GGUF placed locally (compose hook `WORKER_MODELS_DIR`/`WORKER_EXTRA_ARGS`, commit `101026ed5`); a **topology-cold** 4-worker load (fresh slice assignment: Ultra9 11.5 GB / i7-8550U 5.7 / i5 3.0 / 1660 Ti 1.0) completed in **~4 min vs ~24 min** for the earlier pure-stream cold load to the 100-Mbit-class boxes — **~6× when it matters most**, and topology-proof where the `-c` cache is topology-sensitive (fleet churn re-deals slices every load). Not the pure-local floor because ≤10 MiB tensors always stream, hash round-trips are per-tensor, and the slow boxes also have slow disks. Fleet link reality check recorded: `.15` does ~130 MB/s (GbE); the i7-8550U/i5 boxes only ~15 MB/s (100-Mbit-class links) — which makes local sourcing disproportionately valuable there.
- [ ] **39. BUG: EP fleet run - CUDA worker OOMs on a 6.5 GiB buffer alloc, coordinator then loses a DIFFERENT worker (.11) mid-decode** — ⬜ filed 2026-07-15 (user's live EP run, coordinator `llama-ep-fleet` + `llama-rpc-worker-cuda`). Evidence: at the first prompt the coordinator logs `RPC worker '10.5.5.11:50052' failed in get_device_memory: connection lost or worker crashed` → clean Compute error 500 → `rpc_try_surg: waiting up to 120s for worker 10.5.5.11:50052` (containment + surgical arm working as designed). Meanwhile the CUDA worker logs `ggml_backend_cuda_buffer_type_alloc_buffer: allocating 6556.88 MiB on device 0: cudaMalloc failed: out of memory`. THREE questions to answer: (a) **why is a 6.5 GiB buffer landing on that CUDA device at all** — if it is the .15 1660 Ti (6 GB) its EP share/compute reserve exceeds the card (placement/caps: does tensor-mode auto-weight or the hand -ts account for GPU members' compute buffers? cf. #37's fix which covered -sm layer); if it is a V100 worker on the coordinator box, it collides with the coordinator's own `COORD_GPU=0,1` allocation (self-contention — may need a warning when a local worker shares CUDA devices with the coordinator); (b) **worker robustness**: a failed cudaMalloc in alloc_buffer should return remote_ptr 0 and the worker should STAY UP (client handles null cleanly) — if the OOM instead killed/hung the worker process or its connection, that is a worker-side containment gap (same class as #29e/#32); (c) **the cross-worker cascade**: the coordinator lost `.11`, not the OOMing worker — check whether a W2W pull from the dead/stuck CUDA worker wedged `.11`'s serve loop (fused-boundary/W2W path holding a socket while blocked on a peer = a deadlock-by-proxy class the #28 exec-lock ABBA fix was about), or whether get_device_memory just timed out coincidentally. Repro pointers: user's `run-ep-fleet-deepseek.sh` as edited (EP_DEVICES=CUDA0,CUDA1,RPC0,RPC1, EP_TS=3,3,4 - note 4 members / 3 shares = member 4 gets 0); needs the worker-side log tail from BOTH the CUDA worker and .11 at the failure moment (Fleet UI log button or `docker logs`), plus `nvidia-smi` on the OOMing box to see who held the VRAM. Loopback approximation: 2 workers + `GGML_RPC_DEBUG_FAIL_ALLOC` injection on one, watch whether the OTHER worker survives the fused-boundary path.
  - **ROOT-CAUSED (2026-07-16): three independent mechanisms, and none of them is the EP topology.** Evidence: .11's docker log (ssh from the coordinator box works), the coordinator-box worker's log (`llama-rpc-worker-cpu` :50053), shell-history timestamps, GGUF placement math, and code reading. The failing run was `run-fleet-deepseek-autoweight.sh` at 2026-07-15 20:00:40Z (`-sm layer` + `--rpc-discover`, fleet-coordinator compose - no `--rpc-reload`), NOT the EP script (its edited runs were 17:08-17:12Z and their logs are gone); the user had started a local CPU worker on the coordinator box (:50053, `--announce`) at 19:40Z to add this box's CPU to the discovered fleet. Timeline: coordinator up 20:00:43Z (first conn on the :50053 worker) -> .11 starts failing W2W pulls 20:05:12Z -> .11 worker dies 20:14:16Z (docker restarts it) -> load/serve churn until teardown ~20:47Z.
  - **(a) ANSWERED - the 6.5 GiB was the 1660 Ti's LAYER-SPLIT weight share, not an EP/tensor-mode buffer.** Auto-weight bailed (.15 was still on the 4.6 image = unscored; auto-weight requires every worker scored) -> memory-proportional DEFAULT split, which has NO capacity cap: when the model (80.75 GiB weights) exceeds the fleet's summed free memory, every device's share exceeds its free memory by construction (share_i = free_i/sum_free x model). ~5.9 GiB free of a ~74 GiB fleet sum = 6.4 GiB share on a 6144 MiB card -> cudaMalloc refused. For the record, the EP config CANNOT produce this: measured from the v2 GGUF, EP_ONLY mirrors only 2806 MiB onto every member (74304 MiB `_exps` split by -ts, 5579 MiB DSA attention dedicated to the owner), so a zero-share 1660 Ti member carries ~2.8 GiB and fits. FIX: capacity-check the default split (clean per-device "share X MiB > device free Y MiB" error at load, or cap+spill like auto-weight).
  - **(b) ANSWERED - containment works as designed, no fix needed.** `rpc_server::alloc_buffer` returns remote_ptr 0 on a failed alloc and the worker stays up (ggml-rpc.cpp:2555; upstream CUDA alloc returns nullptr with exactly the logged error, no abort); the meta/client side frees partial member allocs and fails the load cleanly. .15's worker was never lost by the coordinator.
  - **(c) ANSWERED - .11 genuinely died, killed by a W2W endpoint-relativity bug amplified by an fd leak.** (1) The coordinator heard the local worker's multicast beacon via LOOPBACK (same-host datagram) and registered it as `127.0.0.1:50053`; (2) every W2W handoff with that worker as source shipped the literal endpoint string to .11 (`COPY_FROM_REMOTE`), which dialed ITSELF -> ECONNREFUSED -> soft-fallback to the bridged copy (correctness preserved; 1050 `[copy_from_remote] cannot reach source worker '127.0.0.1:50053'` lines in 9 min - no negative cache, it re-dials every handoff); (3) `socket_t::connect` LEAKS the fd on every failure path (transport.cpp:657 `::connect` fail, plus the set_no_delay and gethostbyname branches; `create_server`'s bind/listen fails leak too) - the fd is only handed to the RAII holder on success; at the container's 1024 soft nofile limit `accept()` failed; (4) the serve loop treats ANY accept failure as FATAL (ggml-rpc.cpp:4118 `return`) -> worker exit 20:14:16Z -> `restart: always` brought it back -> the coordinator's next command on the dead cached socket (get_device_memory, fleet status poll) printed "connection lost or worker crashed". FIX LIST: (1) ✅ close the fd on all `socket_t::connect`/`create_server`/`accept` failure paths [also an upstream bug]; (2) ✅ accept() failure: log + retry with 200ms sleep (cap 300 consecutive = ~60s, then exit for the supervisor - a wedged listener still recovers via restart policy), never die on one EMFILE [also upstream]; (3) ✅ a loopback source endpoint is never shipped in COPY_FROM_REMOTE to a non-loopback destination - `rpc_prepare_w2w_pull` forces the bridged copy (one-time WARN); loopback-to-loopback W2W (pure local fleets) is preserved; plus a 5s negative cache for unreachable peers in `copy_from_remote` (also bounds the SYN-timeout stall against a genuinely dead host to one dial per 5s). **Gates (2026-07-16, CPU loopback, Qwen3-0.6B):** (A) 2-worker all-loopback `-sm layer`: W2W still engages (100 COPY_FROM_REMOTE calls), temp-0 text byte-identical to the no-RPC reference; (B) mixed addressing (`--rpc 127.0.0.1:P1,10.5.5.1:P2`): guard WARN fires once, COPY_FROM_REMOTE drops to 0 (bridged), text byte-identical to (A); (C) protocol-level hammer (hand-built COPY_FROM_REMOTE): 400 distinct dead src endpoints = 400 real dials, worker fd count FLAT at 4 (was: +1 leaked fd per dial), then 200 pulls from one dead endpoint = exactly 1 dial + 1 log in 3 ms total (negative cache), worker alive; (D) worker under `ulimit -n 32` + 30 held connections: accept fails, logs "(attempt 1, retrying)", worker SURVIVES and serves normally after release (was: process exit - the exact .11 death). (4) ⬜ still pending: capacity-check the default memory-proportional split (clean per-device error when a share exceeds device memory) - the (a) fix.
- [x] **40. BUG: autoweight fleet coordinator SIGSEGV during serving** — ✅ **FIXED same day (core-dump analysis): `gethostbyname` is MT-Unsafe and the fleet-status probes race it.** The systemd core (id 1347006, 6.7 GB, analyzed in gdb against the image's binaries via sysroot+solib-search-path) shows the crashing thread is an **httplib HTTP worker** in `server_routes::init_routes()::lambda#2 -> ggml_backend_rpc_endpoint_reachable -> rpc_probe_endpoint -> glibc NSS internals` — the `/fleet/status` reachability probe, NOT the decode path (the 2nd-request timing was coincidence: the user had the loading/fleet page open, each poll probes every RPC endpoint, and polls overlap across HTTP threads; glibc's `gethostbyname` returns a pointer into shared static state and is documented MT-Unsafe - concurrent calls corrupt its NSS machinery). The crash address sits right past `__nss_database_lookup` in libc, i.e. inside the resolver, and frame 1's return address is the instruction after the `gethostbyname` call. FIX: `rpc_resolve_ipv4()` via `getaddrinfo` (POSIX MT-safe) replaces both `gethostbyname` sites in transport.cpp (`socket_t::connect` + `rpc_probe_endpoint`); bonus hardening: the probe's `select()` now rejects fds >= FD_SETSIZE (FD_SET would write out of bounds under fd pressure) [gethostbyname-in-connect also exists UPSTREAM]. Gate: 60 rounds x 16 CONCURRENT `/fleet/status` polls (960 requests) against a loopback fleet with a killed worker in the probe set - all 200, zero faults, serving intact; loopback repro of the original decode-side theories had already come up clean (3 shapes), consistent with the real cause living on the HTTP side. Original filing: (user's live test, image `7ca338f6b`, container `llama-fleet-coordinator`). Facts: run loaded fine (16 min, 7 RPC + 2 memory-capped CUDA stages; the #39 loopback-W2W guard fired at warmup as designed), request 1 completed clean (58 tok @ ~1.0 t/s, graphs reused 57); request 2 selected slot 3 by LCP similarity (sim 0.795, partial f_keep), logged prompt progress 0.95 at 15:31:17Z, then SIGSEGV ~16 s later (docker die exitCode=139 15:31:42Z; systemd core id 1347006, thread 22 in a libc memcpy-family frame, deeper frames unresolved). NOT the meta backend (layer split - meta not in the path). Loopback repro attempts NEGATIVE (trunc-V4 6L, np4 unified KV, LCP reuse f_keep 0.19-0.87 exercised): 3 all-loopback RPC stages; mixed loopback/LAN endpoints (guard-bridged handoffs live); A/B/C progressive-prefix sequences - all clean. Unmirrored dimensions = prime suspects: (a) local CUDA stages beside a nearly-full prod (VRAM pressure at serving time - a failed runtime alloc, e.g. pinned staging for the CUDA<->RPC bridged copy, -> memcpy to null+offset); (b) full 43L scale / 9 stages; (c) prompt-checkpoint/state memcpy over RPC KV on the partial-reuse path (the 16 s silent gap fits a big state op; checkpoint logs are TRC-level, invisible at -lv 3). NEXT: analyze the core - `sudo coredumpctl dump 1347006 --output=<path>` (+ chown), then gdb against /app binaries extracted from the 7ca338f6b image for the full backtrace. The live restarted coordinator (run 2) will likely reproduce on its next similar request - grab `docker logs` tail + a fresh core id if it does. Note run 1 vs run 2 device-order nondeterminism: discovery ordered RPC0..6 differently across the two runs (beacon arrival order) - -ts positional stability across reloads may deserve its own look. — ⬜ **TENTATIVE / not committed** (idea parked 2026-07-10; decide later whether it's worth the effort). Measured on the Core Ultra 9 285H box (.11): Arc 140T iGPU via Vulkan/ANV = **decode parity with the CPU (10.0 vs 10.4 t/s — both bandwidth-bound on the same LPDDR5X) but prefill 2× WORSE (5.3 vs ~10.7)** — Mesa ANV doesn't use the XMX matrix engines. Suspected headroom: (a) **SYCL backend** (`.devops/intel.Dockerfile`, oneAPI) — Intel's first-class llama.cpp backend, DOES use XMX; needs the same treatment as cpu/vulkan (add `-DGGML_RPC=ON` + an `rpc-worker` target), then A/B SYCL vs Vulkan vs CPU on the same box; (b) **Mesa coopmat** — `VK_KHR_cooperative_matrix` for Intel is landing in newer Mesa; recheck the Vulkan numbers after a Mesa upgrade; (c) Vulkan env knobs (`GGML_VK_*`). Goal: iGPU worker ≥ CPU worker on prefill while holding decode parity (frees the CPU cores at no speed cost).
- [x] **41. Fleet UI: summary strip (layer placement, RAM/VRAM totals) + `--fleet-preflight` bench** — ✅ **DONE (2026-07-16, user request).** The badge row next to "Pipeline devices" is now a summary STRIP of mini stat tiles: split mode, gpu layers (-ngl), **layer placement (N gpu / M cpu, summed from the per-device split)**, **VRAM free/total** and **worker RAM free/total** (client-side aggregation of the existing `/fleet/status` device data; `worker_is_cpu` buckets RAM vs VRAM), and the preflight result. **`--fleet-preflight <gguf>`** (env `LLAMA_ARG_FLEET_PREFLIGHT`; compose knob `COORD_PREFLIGHT`): before the main load (once per process, never on resume/failure reloads), the server loads the small model over the SAME devices/split, times greedy single-token decodes (8 warmup + 16 timed), frees it, and publishes `{tps, model, n_tokens, load_s}` in `/fleet/status` - available DURING the main load, so the loading page shows it while weights stream (added to loading.html's model line too). Honest semantics documented in the tile tooltip: a small dense model's compute is negligible, so the number is the fleet's PER-TOKEN BOUNDARY/LATENCY FLOOR (the term bandwidth scores cannot see) - an upper bound for any model on this topology, NOT a throughput estimate. Failures skip cleanly (WARN, main load proceeds). Gate (loopback, 2 CPU workers, trunc-V4 main + Qwen3-0.6B preflight): bench 21.3 t/s in 3.4 s total (2.2 s load); status carries preflight in loading AND ready states; tile math verified from the JSON (6 cpu / 0 gpu layers, 93.8 GiB RAM, vram tile hides with no GPU devices); serving clean afterward; svelte-check + vite build clean. Autoweight script sets COORD_PREFLIGHT by default (0.6B streams once to cold workers - their -c cache makes reruns instant).
- [ ] **42. Fleet UI: on-demand worker re-score + fresh scores at split time** — ⬜ PARKED (designed 2026-07-16, user: "not yet"). Motivation: `--score` benches ONCE at rpc-server startup, so a worker (re)started while its box is busy under-reads by up to 10x and auto-weight starves it (observed live: the coordinator-box i9 scored 3.43 GB/s during an image build vs ~29-36 idle -> 1 layer). Design (reuses the #35 shape): (a) new RPC command `RESCORE` (proto minor bump to 4.8, served on an ephemeral connection outside the exec lock like GET_SCORE/GET_LOG; worker re-runs the ~0.5 s bandwidth bench, REFUSES with "busy" if mid-compute - a bench racing a decode under-reads both) and the worker updates its beacon payload with the fresh number; (b) coordinator route `POST /fleet/worker/rescore?ep=...` (gated `--fleet-admin` + api-key) + a per-worker button and a fleet-wide "Re-score all" in the UI, updating scores/rank badges live; (c) UI must state that re-scoring does NOT re-split a loaded model - shares apply at the next (re)load; natural flow "Re-score all -> Reload" (#38's reload does the second half); (d) the deeper variant to bundle: `--rpc-auto-weight` calls RESCORE on every worker AT SPLIT TIME instead of trusting the possibly-stale beacon score (~0.5 s/worker on a minutes-long load) - deletes the staleness class outright; the button then mainly serves pre-include eyeballing. Mixed fleets degrade gracefully (pre-4.8 workers fall back to the cached score); needs a worker image roll to take effect.
- [ ] **43. BUG (2 parts): .15 CPU worker died on the 2nd EP request; surgical recovery then HUNG past its 120 s window** — ⬜ filed 2026-07-16 (live EP A/B run, coordinator e0d867ec1, workers 55ec0bfde, config CUDA0 owner + .11 (3) + .15:50054 (2), -np 1). Part A - worker death: request 1 clean (64 tok @ 2.44 t/s = the historical band); request 2 (identical prompt, LCP slot reuse sim 1.000, f_keep 0.100, partial cache keep at pos 49) killed `10.5.5.15:50054` ~19 s into decode ("failed in get_device_memory: connection lost or worker crashed" + graph_compute -1). Same second-request-after-partial-reuse shape as the #40 timing, but #40's resolver fix is IN this build - and this time a worker genuinely died, not the coordinator. NEED .15's `docker logs` tail (set_rows OOB canary now logs node names; also check for OOM-kill - the box had just streamed its 30 GB share, page-cache pressure). Part B - surgical hang: worker's port returned (docker restart) but the coordinator sat >9 min past "rpc_try_surg: waiting up to 120s" with ZERO further log lines; evidence at hang time: TWO ESTABLISHED conns to 10.5.5.15:50054, one with Recv-Q 16400 bytes UNREAD (a response nobody consumed), process alive (Ssl), threads unblocked-visible. Smells like the surgical/probe path blocking on the wrong socket or a mutex convoy with the fleet-status poll (the probe goes through the blocking cached-socket path per its own comment - the cached socket died with the old worker process). Repro pointers: kill a tensor-mode member mid-decode with the fleet UI polling, watch rpc_try_surgical. Recovery workaround: restart the coordinator container (fresh load from warm caches).
- [ ] **27. Intel Arc worker optimization (SYCL / better Vulkan)** — ⬜ **TENTATIVE / not committed** (idea parked 2026-07-10; decide later whether it's worth the effort). Measured on the Core Ultra 9 285H box (.11): Arc 140T iGPU via Vulkan/ANV = **decode parity with the CPU (10.0 vs 10.4 t/s — both bandwidth-bound on the same LPDDR5X) but prefill 2× WORSE (5.3 vs ~10.7)** — Mesa ANV doesn't use the XMX matrix engines. Suspected headroom: (a) **SYCL backend** (`.devops/intel.Dockerfile`, oneAPI) — Intel's first-class llama.cpp backend, DOES use XMX; needs the same treatment as cpu/vulkan (add `-DGGML_RPC=ON` + an `rpc-worker` target), then A/B SYCL vs Vulkan vs CPU on the same box; (b) **Mesa coopmat** — `VK_KHR_cooperative_matrix` for Intel is landing in newer Mesa; recheck the Vulkan numbers after a Mesa upgrade; (c) Vulkan env knobs (`GGML_VK_*`). Goal: iGPU worker ≥ CPU worker on prefill while holding decode parity (frees the CPU cores at no speed cost).
- [x] **38. Fleet UI: "Include" button to enlist a newly discovered worker** — ✅ **implemented + gated-tested on loopback (2026-07-15); needs an image rebuild to ship (the 286d2def1 image predates it) and a live-fleet click-through.** A box that joins the LAN after load shows up in `/fleet/status.discovered[]` with `in_pipeline:false` but contributes nothing — device registration is parse-time, so a JOIN cannot reuse the in-process reload; the correct mechanism is the same one the worker-loss path uses: exit 42 → restart policy + `--rpc-discover` re-split over the grown fleet. Implemented: `POST /fleet/reload` (server.cpp route, handler beside the #35d restart handler; gated `--fleet-admin` AND `--api-key`; optional body `endpoint` is logged; responds `{success:true}` then exits 42 after a 2 s flush) + UI: an "Include" button on `available` rows of the Fleet page's Discovered-on-LAN table (shown only when server_state=ready; disabled without fleet_admin; confirm dialog warns that in-flight requests drop and the model reloads across the fleet — fast when workers have warm caches / `--model-dir`). Files: `tools/server/server{.cpp,-context.h,-context.cpp}`, `tools/ui` (api-endpoints, fleet.d.ts + app.d.ts globals, fleet.service.ts `reloadFleet`, FleetScreen.svelte). svelte-check/lint/build clean; dist rebuilt. Loopback gates: no key → 401; key without `--fleet-admin` → 501 + server stays up; key+admin → `{success:true}`, log names the endpoint, EXIT=42 after 2 s. Note: in docker the restart policy makes 42 a reload; a bare CLI run just exits.
- [x] **37. BUG: V4 `-sm layer` over RPC shipped invalid-ptr graphs (fleet-killer); auto-weight caps ignored KV+compute** — ✅ **FIXED (2026-07-15, working tree; image build pending).** The original diagnosis ("the degraded no-pipeline retry ships invalid graphs") was WRONG — injection experiments on a loopback fleet proved the retry path is innocent; the corruption fires on ANY V4 `-sm layer` multi-worker decode, which is why that combo never worked. THREE independent bugs found and fixed, all reproduced on a 4-worker CPU loopback fleet with a truncated V4 (`LLAMA_TRUNC_ARR=1 --override-kv deepseek4.block_count=int:6`, ~60 s loads, Run-B-like `-ts 4,8,3,0` incl. a zero share):
  - **(1) RPC serializer shipped foreign view nodes** (the fleet-killer). The sched SKIPS view ops when splitting (ggml-backend.cpp pass 5) — they're no-op kernels — so a view whose data lives on another backend can sit positionally inside a foreign split's node range. Local backends never notice (host pointers are process-valid); the RPC serializer shipped them and the worker rejected the WHOLE graph (`[create_node] invalid data ptr`) then dropped the connection — the .25 "crash" in run B was this containment working as designed. DSV4 mass-produces the pattern: the memory-ctx input builder creates `get_kv()` state reshapes for ALL layers up front (they land in split #1's range) and hyper-connections view the PREVIOUS layer's `l_out` (land in the next split's range); dense/GLM/Qwen graphs don't do this, which is why only V4 died. Fix: `collect_local_nodes()` (ggml-rpc.cpp) drops view-op nodes whose buffer belongs to a different endpoint/device from the serialized subgraph — provably inert, since the sched rewired all same-split consumers to local input copies; an all-foreign (empty) subgraph skips the wire entirely. Coordinator-only fix — deployed workers keep working unchanged.
  - **(2) gallocr aborted on zero-byte tensors in empty chunks** (`GGML_ASSERT(tensor->buffer == NULL)`, first prompt, decode-graph cache ON): a small-ubatch V4 graph can place ONLY zero-byte tensors on a device (empty state-read slices + their input copies; measured `alloc_size=0`, chunk `max_size 0`) → zero-size chunk = dummy buffer with NULL base → first init leaves `data == NULL` (legal for 0 bytes) → the same copy tensor appears as a node of TWO splits → the second init tried to re-allocate it and hit the assert; the follow-on `ggml_backend_view_init` assert (`view_src->data != NULL`) is the same class. Fix: `ggml_gallocr_init_tensor` treats `buffer != NULL` as already-initialized (idempotent re-init), `ggml_backend_view_init` accepts NULL view_src data for zero-byte views, and the NULL-base log fires only when a >0-byte tensor lands in an empty chunk (real corruption).
  - **(3) auto-weight caps ignored KV + compute** (run A's clean OOM — the part of the original filing that was right): `apply_rpc_auto_weight` now reads the GGUF header for KV dims (MLA-aware via `attention.kv_lora_rank` + rope dims; per-layer `head_count_kv` arrays for hybrids; `-ctk/-ctv` type sizes) and scales the caps from weight space to weight+KV space, plus a flat per-device compute allowance = 2% of the model clamped to [512 MiB, 2 GiB] (env override `LLAMA_RPC_AUTO_WEIGHT_RESERVE_MB`), logged as `reserving N MiB KV total + M MiB compute headroom`. Verified: Qwen3-0.6B KV estimate EXACT (896 MiB @ 8k ctx = 28L x 8 kv-heads x 128 x 2 x f16), V4 MLA path sane (1024 dims/layer/token), too-tight fleets still bail safely to the default split.
  - **Hardening landed alongside:** `sched_reserve` clears `sched_need_reserve` only on SUCCESS (a thrown reserve used to leave the context claiming "reserved", so the next decode computed on never-allocated buffers); worker `create_node` rejection now logs tensor name/op/buffer handle; client logs when a worker returns NULL base for a known buffer. Debug enablers kept (env-gated): `GGML_RPC_DEBUG_FAIL_ALLOC=EP:SKIP:COUNT` (fail the COUNT allocs after SKIP on endpoint EP - reproduces compute-buffer alloc failures), `GGML_RPC_DEBUG_BUF=1` (buffer alloc/free lifecycle log).
  - **Gates passed (loopback, truncated V4 + 4 workers):** cache-ON load + serve clean, 3x temp-0 completions byte-identical to a single-CPU no-RPC reference; decode-cache-OFF also clean; Run-B injection scenario (PP compute alloc fails → non-PP retry → serve) end-to-end clean; Qwen3-0.6B 2-worker regression unchanged ("Paris. The capital of Italy is Rome"). Zero errors in coordinator + all 4 worker logs.
  - **REAL-FLEET VALIDATION PASSED (2026-07-15 evening, image `94b7ba5dc`, full 86.7 GB V4, 6 RPC devices + 2 V100s):** auto-weight logged `reserving 344 MiB KV total (n_ctx 4096) + 1654 MiB compute headroom per device`; split shifted as designed (V100s 32.7% -> 30.6% each, .15's 6 GB GPU 3.4% -> 1.7%, both memory-capped); load + warmup + first prompt + a 262-token generation all clean - ZERO invalid-ptr / connection-lost / alloc-failure lines (run A and run B failure modes both eliminated). Decode 1.5-1.7 t/s = the expected naive `-sm layer` baseline over 100-Mbit links (#31 sum-of-stages), not a regression - the EP topology (#28) remains the perf path.
  - **Residuals:** (i) `common_context_can_seq_rm`'s probe decode failure only downgrades, so the server logs "model loaded" even after a fleet-wide decode failure — acceptable for a probe, revisit if it masks faults; (ii) the old #30 "zero -ts share segfaults" again did NOT reproduce (zero-share worker loaded and served fine).
- [x] **36. Fleet UI: visually differentiate GPU vs CPU workers (icons / postfix)** — ✅ **DONE (2026-07-15, `a51d60eb5`), verified live on a dual-device GPU+CPU worker.** Kind icons (lucide Gpu/Cpu; inlined SVG on the loading page) on Fleet cards + loading rows; `box 1/2 / 2/2` badges group same-endpoint siblings; `(gpu)/(cpu)` postfix in the --rpc-auto-weight split table; `worker_is_cpu` added to the load plan / load_workers[]. Landed alongside: the per-endpoint score is no longer SHOWN on a GPU worker's CPU sibling (both status paths) and fastest/slowest ranks key by device name, not endpoint — the UI counterpart of the #35f sibling rule. Original filing: Today a GPU-exposed RPC device and a worker-CPU device look alike on the Fleet page and the loading page — same card, only a small "CPU (RAM)" badge distinguishes them, and a dual-device worker like .15 (`-d CUDA0,CPU`) renders as two near-identical cards on one endpoint. Make the device kind legible at a glance: (a) per-device **icon** (lucide `Gpu`/`Cpu` — check icon availability; fall back to `Microchip`/`MemoryStick`) on the card title in `FleetDeviceCard.svelte` AND on the loading page's worker rows (`tools/ui/static/loading.html` — inline SVG there, no lucide); (b) optionally a name **postfix** (`RPC3 (gpu)` / `RPC4 (cpu)`) where icons don't fit (log lines, `--rpc-auto-weight:` table in `common/arg.cpp`, discovered-on-LAN table). Data is already in `/fleet/status`: `worker_is_cpu` per device (and `is_rpc`/`endpoint`); the loading page's `load_workers[]` does NOT carry `worker_is_cpu` yet — add it from the load-plan snapshot (`fleet_capture_load_plan` in `server-context.cpp` can resolve `ggml_backend_rpc_dev_worker_is_cpu` per device like `apply_rpc_auto_weight` does). Bonus: group same-endpoint sibling devices visually (shared border or "2 devices @ 10.5.5.15" header) so a GPU+CPU worker reads as one box with two roles — pairs with the #35f sibling rule (CPU sibling gets no scored share, its role is KV annex). UI-only + one small field addition; no proto change, no worker roll.
- [x] **35. Fleet web UI: "RPC fleet" section in the server web UI + `/fleet/*` API** — ✅ **DONE (2026-07-14, all six sub-tasks), verified live on a 2-worker loopback fleet with Qwen3-0.6B.** Proto bump 4.6→4.7 (`GET_LOG`/`SHUTDOWN`/`GET_SCORE`, all served OUTSIDE the worker exec lock so a busy/hung worker still answers); per-endpoint transfer counters in the RPC client (`rpc_ep_stat`, always on); worker log ring (64 KiB, `ggml_log_set` tee in rpc-server); `--score` startup benchmark (chained f32 matvec = effective memory bandwidth, the decode-bound quantity) published via beacon `bw_gbps=` field + RPC; coordinator `--rpc-auto-weight` fills unset `-ts` ∝ score water-filled against free memory (GGUF file size as weight proxy, #31 law-1 warning included); server: `GET /fleet/status` (exempted from the loading-503 → live fleet-wide **load progress**; layer map snapshotted on the model thread at load end; background beacon listener caches LAN workers), `GET /fleet/worker/log`, `POST /fleet/worker/restart` (gated `--fleet-admin` AND `--api-key`); UI: Fleet section (`#/fleet`, worker cards with fastest/slowest badges, memory bars, MB/s + EWMA latency + SVG sparklines, follow-mode log sheet, confirm-dialog restart), svelte-check/lint/build clean. **Live gates passed:** status during load + ready; auto-weight split 49/51 by score then 10/90 after one worker rebenchmarked solo (scores drive the split end-to-end); completion over the split fleet; log tail over RPC mid-serve; restart: 401 without key, 404 unknown ep, success → worker exited → `--rpc-reload` recovered in <10 s and completions resumed. Compose: workers `--score` by default; coordinator knobs `COORD_AUTO_WEIGHT` / `COORD_FLEET_ADMIN` / `COORD_API_KEY`. Docs: guide §2. **Residuals:** (1) beacon-listener TCP probes churn worker logs (~1 conn/10 s/worker) — cosmetic; (2) `--score` benchmarks that start concurrently on ONE box contend and under-read (real fleets: distinct machines, non-issue; rerun of a solo worker read 37 vs 4 GB/s under 20-thread contention); (3) auto-weight bails (with warning) on `LLAMA_KV_WORKER_HOST`, `-sm tensor`, unscored workers, or explicit `-ts`. Original filing below:
  - (a) **`GET /fleet/status` + loading state.** Per device: name (`RPC[host:port]` / `CUDA0` / annex), endpoint, free/total memory, split share + layer range assigned, reachability. Global: server state incl. `rpc_reload_pending` / surgical-wait ("waiting up to Ns for worker X"), and **load progress across the fleet** — hook the model-load progress callback and, for the streamed first load, per-endpoint bytes-uploaded counters in the RPC client (`ggml/src/ggml-rpc.cpp` SET_TENSOR path) so the UI can show which worker is still receiving weights and at what MB/s (link speeds vary wildly across the fleet — #31 measured 15 vs 130 MB/s). UI: fleet overview card with per-worker load/serve state.
  - (b) **Worker list + device inventory.** What each worker *exposes* vs what the model *uses*: worker→devices mapping (a worker can contribute several devices: `CUDA0`, worker-CPU KV annex from #30 (`CPU|` desc marker), `Meta[N]` TP islands), plus discovered-but-unused workers — cache the last beacon set (`common/arg.cpp` `discover_rpc_devices`) and re-listen periodically server-side so the UI can also show "present on LAN but not in the pipeline" (feeds #29d dynamic join).
  - (c) **Per-worker logs in the UI.** `rpc-server` keeps an in-memory ring buffer (~64 KB) of its stderr lines; new fork RPC command `GET_LOG` (proto minor bump, same pattern as the batched-pull commands from #28) lets the coordinator fetch the tail; `GET /fleet/worker/log?ep=…` proxies it; UI: click a worker → log pane with follow mode. Zero extra ports/sidecars; log fetch must go over a separate connection or the exec lock (#28 ABBA fix) — don't block the compute socket.
  - (d) **Restart a worker from the UI.** `POST /fleet/worker/restart` → new RPC command `SHUTDOWN` (worker replies-then-`exit(0)`; docker `restart: unless-stopped` brings it back; #29c surgical re-provision then re-provisions it automatically — measured 121 s to byte-identical recovery, so a UI restart is already safe end-to-end). Gate: off unless `--fleet-admin` AND an API key is set (RPC itself is unauthenticated; don't add a remote-kill primitive silently), UI confirm dialog.
  - (e) **Per-worker performance stats.** Coordinator-side (no protocol change): per-endpoint EWMA of RPC call latency, bytes in/out, and per-pipeline-stage wall time from the existing graph timing hooks; expose in `/fleet/status`; UI: sparkline + MB/s + "stage time share" per worker — makes the pipeline's slowest stage visible at a glance, which is the #1 diagnostic for a mis-weighted split (#31).
  - (f) **Worker speed score + env-gated auto-weighted split** (the follow-up already filed in #31/#29d). **Verdict on the proposal: better than the current default — do it.** The default split is free-memory-proportional (`llama-model.cpp:1466`) — capacity-safe but speed-blind; #31 measured it at **~2× worse** than speed-weighted in the CPU-fleet regime (memory-default 1.2 t/s vs bandwidth-proportional 2.26 t/s optimum on the real fleet) and established the right weight is **measured memory bandwidth**, not RAM size. Refinement vs the "run Qwen-0.6b on each worker" idea: default scorer = a seconds-long in-worker microbenchmark (STREAM-like bandwidth probe + one small matmul for GPU devices) run at `rpc-server` startup and carried in the discovery beacon (score field beside free-mem) — no model download, works on bare workers; keep a `qwen-0.6b` end-to-end mode as optional calibration (needs the model worker-local via #26 `--model-dir`; measures the same thing but in minutes and validates the probe). Coordinator (env `LLAMA_ARG_RPC_AUTO_WEIGHT=1`): `-ts` ∝ score **capped by free memory** (capacity stays a hard constraint; overflow spills to next-fastest), explicit `-ts`/`COORD_TS` always wins; plus the #31 law-1 guard — if the model fits local fast VRAM, WARN that distribution will lose. UI tie-in: show each worker's score + rank (fastest/slowest badges) in the fleet section, and the resulting split next to it, so a bad balance is visible before the first token.
- [x] **34. BUG: meta backend asserts at load when `-sm tensor` is combined with CPU-hosted weights (`-ncmoe` / partial `-ngl`)** — ✅ **FIXED (2026-07-15): supported, not rejected — same root cause class as task 37.** The sched skips view ops when splitting, so views of NON-meta tensors (CPU-hosted `-ncmoe` experts, partial `-ngl` CPU-layer outputs) sit positionally inside the meta split's node range; `ggml_backend_meta_graph_compute` materialized shadows for every node and asserted on the foreign buffer. Fix: pre-filter foreign-view nodes at the top of meta graph_compute (deterministic, so uid/content build caching stays valid) — the mirror of task 37's RPC `collect_local_nodes()` filter; the old narrow s_copy_main workaround is subsumed. Repro required BOTH partial `-ngl` AND `-ncmoe` (each alone worked). Gates (loopback, 2 RPC workers): exact repro shape (trunc-V4 `-sm tensor -ts 0.5,0.5 -ngl 4 -ncmoe 3 --no-mmap`) now runs and its temp-0 output is BYTE-IDENTICAL to the single-CPU no-RPC reference; regressions byte-identical: V4 `-ngl 99`, V4 `-ngl 99 -ncmoe 3`, Qwen3-0.6B dense tensor mode. Original filing:  Repro: single-box ncmoe profile (`docker-compose.ncmoe.yml`, working-tree edit, image `llamacpp-local-v100:latest` = `026ea0ae9`) with `-ngl 30 -sm tensor -ts 0.5,0.5 -ncmoe 13 -fit off --no-mmap` on V4-Flash: server dies during `common_init_from_params`'s warmup decode (the edit also dropped `--no-warmup`, so the first decode fires at load) with `ggml/src/ggml-backend-meta.cpp:1612: GGML_ASSERT(ggml_backend_buffer_is_meta(tensor->buffer))` inside `ggml_backend_sched_graph_compute_async`; the failing frame is `ggml_backend_meta_buffer_init_tensor_impl` (lines 773/1601 carry the same assert on sibling paths). Trigger analysis: with `-sm tensor` the meta device owns the split stack, but `-ncmoe 13` puts the first 13 layers' experts in a plain CPU buffer and `-ngl 30` leaves the upper layers on CPU too — the sched hands the meta backend a tensor whose buffer is a non-meta (CPU) buffer, and the init/split-state path asserts instead of falling back or erroring cleanly. Same failure class as the recorded task-10 residual ("unsupported-op CPU fallback in tensor mode aborts instead of erroring cleanly", meta.cpp:557). The committed profile (`-ngl 99 -ncmoe 20`, plain layer split, no `-sm tensor`) is unaffected — the 3.54 t/s single-box baseline stands. Decide: (a) support the combo — meta treats non-meta-buffer leaves as host-resident inputs, the way the ncmoe CPU experts already work under layer split; or (b) reject it cleanly at load with a real error message ("`-ncmoe` / partial `-ngl` not supported with `-sm tensor`") instead of a mid-warmup GGML_ASSERT. Until fixed: keep the ncmoe profile on `-ngl 99` layer split (or revert the compose edit).
- [x] **33. INFRA: local Docker registry on the V100 box (workers pull instead of build)** — ✅ **DONE (closed 2026-07-15: user confirmed every worker box carries `{"insecure-registries": ["10.5.5.1:5000"]}` in daemon.json; the pull flow itself was already proven 2026-07-10 when .15 pulled `75-e08854d4b` for the #32 validation).** Coordinator side (2026-07-10): `docker-compose.registry.yml` (registry:2, `restart: always`, volume `llamacpp-registry-data`, delete-enabled) running, bound to `127.0.0.1:5000` (push side — localhost is docker's only TLS-exempt name, and adding insecure-registries HERE would need a dockerd restart that kills the prod containers) + `10.5.5.1:5000` (LAN pull side); round-trip verified (pushed `llamacpp-cpu:rpc-worker` via localhost, catalog + tags visible on the LAN endpoint). Full flow documented in `docs/dev-workflow.md` §1b. **Remaining: per-worker one-time setup** — add `{"insecure-registries": ["10.5.5.1:5000"]}` to `/etc/docker/daemon.json` on .15 and .11 + dockerd restart (needs hands on the boxes; no ssh from here), then pull-test one image. Original request (user 2026-07-10): Building the images on the slower fleet boxes is painful (the i7-9750H takes far longer than this box, and every worker rebuild repeats identical work). Set up a local registry hub on THIS machine so other boxes just `docker pull` the images built here; local builds remain only for genuinely box-specific targets (e.g. a future Intel Arc SYCL image, or another arch this box can't cross-build). Sketch: (a) run the standard `registry:2` container on the V100 box (persistent volume, `restart: always`, bind to the LAN IP, port 5000); (b) workers add `{"insecure-registries": ["<v100-ip>:5000"]}` to `/etc/docker/daemon.json` (private trusted LAN — same threat model as the RPC protocol itself) + daemon restart; (c) push flow: `docker tag llamacpp-cuda:75-<tag> <v100-ip>:5000/llamacpp-cuda:75-<tag> && docker push ...`, worker: `docker pull <v100-ip>:5000/llamacpp-cuda:75-<tag>`; (d) note CUDA arch cross-builds are trivial from here (`CUDA_DOCKER_ARCH=75` for the 1660 Ti — the ARG is wired since task 22), CPU/Vulkan images are arch-independent (GGML_NATIVE=OFF variants) or need `-march` care — check `cpu.Dockerfile`'s `GGML_CPU_ALL_VARIANTS=ON` covers the i7/Ultra9 (it does — runtime dispatch); (e) disk watch: registry volume grows per tag — prune policy (`registry garbage-collect` or keep-last-N tags). Document the full flow (registry setup, worker daemon.json, push/pull commands, when a local build is still required) in `docs/dev-workflow.md` once working.
- [x] **32. BUG: FA MMA kernel crashes on GTX 16xx (TU116/117 — cc 7.5 WITHOUT tensor cores)** — ✅ **DONE; CONFIRMED ON THE REAL 1660 Ti (2026-07-10 evening).** Worker at .15 pulled `10.5.5.1:5000/llamacpp-cuda:75-e08854d4b` from the #33 registry; the exact former crash config (35B, 5 layers on the 1660 Ti, **FA ON**, CPU coordinator) now completes coherently at **10.5 t/s — FASTER than the 7.8 t/s `-fa off` workaround** — and the worker process survives. Residual resolved (same evening): the worker log shows **NO fallback WARN** across both runs — including a stress run with warmup + 1.6k-token prefill (86.2 t/s pp / 10.2 tg, exit 0, worker alive) that exercises the largest MMA smem configs. So on the current image, **TU116 accepts the MMA kernel's shared-memory opt-in outright and runs the MMA FA path via its emulated HMMA** — the historical abort does not reproduce with today's smem tuning tables + CUDA 12.8 (the old worker image's build requested more than TU116's envelope, or the older driver/toolkit rejected it). The fallback machinery stays as armor: if any future config over-asks, the worker now degrades to the tile kernel instead of dying, which is exactly the #29(e) containment property for this error class. Upstream check came first per the plan: NO fix exists upstream (master still gates MMA purely on `turing_mma_available(cc)`; maintainer position in discussion #5329 is that tensor cores can't be queried via the CUDA API, only name/cc heuristics). Fix shipped here, probe-based (no name matching): (1) `fattn-mma-f16.cuh` — the `cudaFuncSetAttribute(MaxDynamicSharedMemorySize)` call is no longer `CUDA_CHECK`-fatal: on failure it logs a one-time WARN carrying the requested bytes + the device's `smpbo` (the diagnostic we lack for TU116), marks the device via new `ggml_cuda_fattn_mma_disable()`, and dispatches THIS call to the tile kernel (safe: tile has identical f16-K/V alloc geometry to MMA, so alloc/compute stay consistent); the duplicated softcap-branch setattr blocks were hoisted into one probe (per-kernel-symbol flag kept, `[2][GGML_CUDA_MAX_DEVICES]`). (2) `fattn.cu` — the kernel picker's turing AND volta MMA branches skip marked devices (`ggml_cuda_fattn_mma_disabled()`, decl in fattn-common.cuh); `GGML_CUDA_FA_NO_MMA=1` pre-marks all devices (worker-side knob — the TU116 worker keeps FA without fleet-wide `-fa off`); `GGML_CUDA_FA_MMA_FORCE_SMEM_FAIL=1` = fault injection to exercise the path on healthy hardware. (3) **Bonus latent-crash fix exposed by (2)**: `ggml_cuda_should_use_wmma_fattn` returned true for NVIDIA Volta but this tree compiles WMMA device code ONLY for HIP+rocwmma (`fattn-wmma-f16.cu:48` guard) → with MMA disabled the picker fell through to WMMA and died with `NO_DEVICE_CODE` ("no device code compatible with CUDA arch 700", async "unspecified launch failure"); the helper now returns false for NVIDIA-Volta with a comment. Also added the missing `cudaErrorInvalidValue` HIP shim (vendors/hip.h). **Verified (V100, Qwen3-0.6B, 300-token prefill so the MMA path engages, temp 0): baseline / NO_MMA / injected-failure all exit 0 and generate IDENTICAL text (~260 t/s each); injection fires exactly one WARN then disables per-device (was: process abort).** Remaining: confirm on the real 1660 Ti — build the `CUDA_DOCKER_ARCH=75` worker image (pull via the #33 registry), run 35B layers with FA on, expect the WARN + tile fallback and capture the logged smpbo/requested-bytes to finally size TU116's real envelope; then also close the #29(e) sub-case this crash exemplified.
- [x] **31. RESEARCH: smallest computable chunk for balanced heterogeneous distribution** — ✅ **DONE (2026-07-11): measured on the real fleet; laws + policy below.** **The split-policy curve** (35B-A3B, 2 hetero CPU workers i7-8550U/DDR4 + i5-3210M/DDR3, CPU coordinator, decode t/s): all-on-fastest **0.88** · speed-weighted 5:2 **1.33** · memory-default 2:1 **1.2** · **bandwidth-proportional 3:2 = 2.26 (optimum)** · even 1:1 **1.72**. **Laws established:** (1) *GPU-rich regime*: never distribute a model that fits fast VRAM (V100s alone 108 t/s vs 1.9 across all 5 machines — sum-of-stages + hops). (2) *CPU-fleet regime, single stream*: the pipeline **aggregates memory bandwidth** (decode is bandwidth-bound), so distribution HELPS and the optimal `-ts` is proportional to per-box **memory bandwidth** — not RAM size (the default; 2× worse) and not CPU specs. all-on-fastest is the WORST config in this regime — the naive "degenerate optimum" theory only holds when one box's bandwidth dominates the sum. (3) *MoE batching on CPU*: `-np 4` aggregate only +27% (1.83→2.33 t/s) — batched tokens route to distinct experts so expert reads don't amortize; batch-throughput scaling is NOT the CPU fleet's lever. (4) *Granularity*: layer granularity (~2% steps at 48 layers) is fine enough for pipelines once weighted right; finer sub-graph chunks cannot change the sum-of-stages law. **The qualitatively better unit is the expert (#28): composition law becomes MAX-over-contacted-workers instead of SUM-over-stages** — a slow box owning few/cold experts adds capacity while rarely gating a token (user's "smallest computable chunk" intuition, confirmed by this analysis; #28 will measure it). **Policy recommendations:** default memory-proportional stays (capacity-safe); CPU fleets should hand-set `-ts` ∝ measured per-box bandwidth (documented in guide §3b); follow-up idea: the #29(d) beacon gains a measured-bandwidth score so the coordinator can auto-weight (filed as part of #28/#30 work). Historical context below. **First real data point (2026-07-10 evening, fully discovered fleet — zero static config):** 35B-A3B `-sm layer` across i7-8550U/32GB (14.2 GB slice) + i5-3210M/16GB (7.0 GB slice), CPU coordinator: decode **1.2 t/s**, prefill 1.9 — the memory-proportional default hands a third of the model to a 2012 dual-core and the pipeline pays sum-of-stages for it. Also: first-load stream to these boxes ran ~15 MB/s (≈24 min for 21 GB; the .15 laptop did 130 MB/s — link speeds vary wildly across the fleet → #26 `--model-dir` matters even more). Baseline for the speed-weighted `-ts` experiment. **Data point 2 (same evening, ALL 5 machines / 6 devices):** 35B across 2×V100 (3.6+3.7 GB) + Ultra9 (7.5) + i7-8550U (4.0) + i5-3210M (2.0) + 1660 Ti (0.5), fully discovered, load ~85 s (caches warm): prefill 4.6, decode **1.9 t/s** — vs **108 t/s** for the same model on the two V100s alone. Quantifies the core single-stream law: pipeline time = SUM of stage times + hops, so adding slow boxes to a model that fits on fast hardware is a large net LOSS; memory-proportional splits amplify it. Distribution pays off for (a) capacity (models that don't fit anywhere), (b) multi-stream throughput, (c) #30/#28-style designs where slow boxes hold rarely-touched state instead of hot-path stages. Goal: find the work granularity at which a fleet of UNEQUAL machines stays balanced (slow boxes get chunks small enough to keep pace instead of bottlenecking the pipeline). Candidate granularities, coarse→fine, with what we already know: (i) **layer** (today's `-sm layer -ts`): 48 layers ≈ 2% granularity — likely already fine enough IF the split is speed-weighted; the observed imbalance (2-worker 35B ran at the slow box's pace) came from the memory-proportional DEFAULT ignoring speed, not from chunks being too big. Cheap first experiment: hand-tuned `-ts` ∝ measured per-box t/s, check the pipeline approaches the harmonic ideal. (ii) **attention/FFN half-layer**: doubles granularity, same activation-passing cost — needs graph-split support. (iii) **expert** (MoE, task #28): per-token dynamic, the natural fine unit — a slow box holding fewer/colder experts contributes without gating the token; granularity ≈ 1/(n_layers×k) of a token's work. (iv) **sub-matmul row slices** (tensor parallel): finest, but every layer needs a cross-machine reduce — task 17 measured even INTRA-box reduce overlap losing on Volta, and Ethernet AllReduce is ~100× worse; presumed dead end, document why rather than re-test. Also to investigate: dynamic rebalancing (work-stealing between workers vs static splits — per-token expert routing is effectively this for free), and whether a per-worker micro-benchmark at connect time (composes with #29 discovery beacon: workers report a speed score) can drive the split automatically. Deliverable: a doc section with the measured granularity/overhead curve on the real fleet + a recommended default policy.
- [x] **30. Worker-local GPU+CPU mix: KV cache in worker RAM, weights in worker VRAM** — ✅ **DONE (2026-07-11): "KV annex" implemented + gated on loopback.** Mechanism (no protocol change): the worker exposes its CPU alongside the GPU (`rpc-server -d CUDA0,CPU` — pre-existing flag); the coordinator (env `LLAMA_KV_WORKER_HOST=1`) detects worker-CPU RPC devices via a new side channel (worker prefixes `CPU|` in the GET_DEVICE_DESC reply — same fork pattern as `Meta[N]`; client strips it, exposes `ggml_backend_rpc_dev_worker_is_cpu` by proc-address so the device still reports type GPU and pure-CPU workers keep working), then (a) `llama_prepare_model_devices` reserves a worker-CPU device with a same-endpoint GPU sibling as a **KV annex** — excluded from layer placement; (b) `llama-kv-cache` allocates the KV of that worker's GPU layers on the annex buft (worker RAM); (c) `llama_context` creates the annex backend so the scheduler can run ops on annex-hosted tensors (found the hard way: "pre-allocated tensor in a buffer that cannot run the operation"). Attention for annexed layers runs on the worker CPU; GPU↔RAM handoffs stay worker-local. **Gate (loopback, 0.6B @ 32k ctx): weights 1137 MiB on worker-GPU, KV 3584 MiB on worker-RAM (annex line in logs), coherent output; decode 33.7 vs 36.1 t/s baseline (~7% cost) — 3.5 GB of VRAM freed for weights.** **Fleet-CONFIRMED (2026-07-11, image `44e70252a` on .15)**: `reserving RPC1 (i7-9750H CPU @ 10.5.5.15) as a KV annex` fired for the real worker; 35B `-ngl 10` put **5.2 GB weights on the 6 GB card (90% VRAM to weights)** with the attention KV (32 MiB — hybrid model, tiny KV; dense models are where the GBs move, per the loopback gate) in the laptop's RAM; coherent 8.5 t/s. **Edge found while baselining (filed, separate)**: `-ts 1,0` (a ZERO-share RPC device) segfaults after memory setup with annex OFF — pre-existing zero-share edge, not the annex code (inert with env unset); repro: `--rpc <worker-with-2-devices> -ngl 10 -ts 1,0`. Helpers: `llama_kv_worker_host_enabled`/`llama_rpc_kv_annex_for` (llama-model.cpp). Original spec below. Small-VRAM GPU workers (e.g. the 6 GB 1660 Ti) waste their capacity on KV+compute buffers: today a layer's weights AND its KV live on the same device, and the only alternatives are per-layer GPU/CPU splits (`rpc-server -d CUDA0,CPU`) or the coordinator-hosted `--no-kv-offload` (KV would cross the network every layer — useless remotely). Wanted: a worker-side placement policy (e.g. `rpc-server --kv-host`) that allocates the KV buffers of its GPU layers in pinned WORKER RAM while weights stay in VRAM — attention either reads KV over PCIe (decode reads are small) or routes attention ops to the worker CPU while matmuls stay on GPU. Payoff for the 1660 Ti box: VRAM holds ~2× the weight layers (the whole 5.7 GB for weights, KV in 64 GB RAM), turning a marginal worker into a real pipeline stage. Note: the single-box equivalent (`-nkvo`) already exists upstream, so the semantics are proven; the work is making the placement observable per-device through the RPC boundary.
- [x] **29. Distributed: worker fleet management (down node → redistribute)** — ✅ **CLOSED (2026-07-15): (a)(b)(c)(d)(e) all done.** Final (c) gap found+fixed while re-verifying on today's tree: `rpc_reload_and_resplit` only filtered unreachable devices when `--device` was EXPLICIT — with plain `--rpc`/discovery (i.e. the real fleet) `params.devices` is empty, llama re-discovered the dead endpoint at reload and every attempt failed on its buffers (the 07-13 matrix must have run with --device; the fleet soak passed via surgical, masking it). Fix: when devices is empty, synthesize llama's default list (RPC GPUs first, device_id-deduped locals, iGPU only without dGPUs, trailing nullptr) so the reachability probe filters it — positional -ts stays aligned by construction; `rpc_try_surgical` similarly probes all registered RPC devices when params.devices is empty (it never armed for --rpc-only configs). Surgical on layer-split topologies falls back to the full reload by design (meta member buffers only) — now logged explicitly. Re-verified kill matrix (loopback 0.6B, 2 workers, --rpc-reload): permanent death -> 500 + "dropping unreachable device RPC1" + reload onto survivor, same PID, byte-identical; killed-then-returned worker -> surgical attempt, clean fallback, recovery, same PID, byte-identical. Real-LAN multicast discovery: confirmed implicitly by the 2026-07-15 fleet runs (5 workers discovered through the physical switch). Surgical default-ON since the fleet soak (opt-out LLAMA_RPC_NO_SURGICAL). History below:  — 🔄 in progress; **(a) DONE (2026-07-10)**. Today there is NO *runtime* fault handling: a dead worker aborts the coordinator mid-session (guide §7). Wanted, in increasing difficulty: (a) ✅ (b) ✅ (see below) (c) ⬜ — (a) ✅ **load-time degradation — DONE (2026-07-10)**: new `--rpc-skip-unavailable` flag (env `LLAMA_ARG_RPC_SKIP_UNAVAILABLE`; `common/arg.cpp` + `common/common.h`) — unreachable `--rpc` servers are dropped with a WARN (per-server + summary + a `-ts`-is-positional caveat) and the default memory-proportional split recomputes over the survivors + local devices; all-workers-down degrades to local-only (loudly); "model no longer fits" surfaces through the normal allocation error path. Default behavior unchanged (fail-fast, now with a hint naming the flag). Order-independent by construction: the flag is pre-scanned (argv + env) in `common_params_parse_ex` before any handler runs, because the `--rpc` handler registers devices the moment it executes (possibly during env-var handling); registration stays parse-time-immediate on purpose — deferring it would break `--device RPC[...]`/`-ot ...=RPC[...]` name resolution. Verified (CPU loopback, live worker :50900 + dead :50999, 0.6B): 7-case matrix all pass — fail-fast unchanged, flag before/after `--rpc`, env×env, all-dead→local-only, happy path; survivor genuinely used (all layers assigned `RPC0`); warnings visible on `llama-server` at default verbosity (the new CLI hides parse-time WRN by design — `cli.cpp:370` starts at LOG_LEVEL_ERROR; ggml's own `E Failed to connect` still shows there, and `-v` placed *before* `--rpc` reveals the full warnings). Docs: guide §6/§7, `env-gates.md` §7. Remaining: (b) ✅ **coordinator drop-survival — DONE (2026-07-11)**: all 18 client-side `RPC_STATUS_ASSERT` abort sites converted to endpoint-poisoning containment (`rpc_mark_failed`, one actionable log line; ops on a dead worker degrade to no-ops with zeroed outputs; `graph_compute` fast-fails poisoned endpoints so decodes return clean errors); fixed the SIGPIPE process-kill hiding behind the old aborts (transport sends now `MSG_NOSIGNAL`); llama-server on a lost worker sends proper error responses to in-flight requests (verified: HTTP client received `{"error":{"code":500,"message":"Compute error."}}`) then exits code 42 after a 2 s flush window (new `ggml_backend_rpc_any_endpoint_failed` proc-address API) — with `restart: always` + `--rpc-discover` the coordinator reloads and re-splits across live workers automatically = self-healing fleet (a working sub-case of (c)). Verified on loopback: worker killed mid-generation → clean per-request error, no abort/core/SIGPIPE; restart+rediscovery cycle recovers; server exit 42 with flushed 500. Deliberate scope note: a connection lost between a successful compute and the logits read can produce one zero-logits token before the failure surfaces (void backend APIs) — closed by (c)-era work. In-place reconnect without reload is NOT possible under per-connection buffer ownership (the worker frees everything with the connection) — that redesign is (c); (c) **runtime redistribution** — on permanent worker death, re-load the lost layer shares onto surviving devices and resume (KV for lost layers is gone → affected slots restart their generations; needs the scheduler to support re-splitting a live model). **DONE (2026-07-13) as in-process reload-and-resplit: new `--rpc-reload` server flag (env `LLAMA_ARG_RPC_RELOAD`).** On worker loss the server fails in-flight requests (clean 500s), unloads the model via the sleeping-state machinery, clears the RPC failure marks (new reg procs `ggml_backend_rpc_reset_failed_endpoints` / `ggml_backend_rpc_dev_reachable`), probes every configured device and reloads across the reachable ones - dead workers drop out WITH their positional -ts shares; the pre-failure device list is kept as the probe base so a worker that returns is re-included by the next failure-triggered reload; if no worker is reachable it loads local-only (loudly, matching --rpc-skip-unavailable semantics), and if even that fails (fleet-sized models) it retries every 10s until a load succeeds. Same PID, HTTP state and queue throughout - no restart policy needed (default behavior unchanged: exit 42 for the restart loop). Closed the #29b known gap on the way: a worker lost between a successful compute and the logits read yielded ONE silent zero-logits token (ret==0) - decode now promotes any_endpoint_failed to a compute error before sampling. ALSO found+fixed a pre-existing SEGFAULT the kill-tests exposed: ggml_backend_sched ignored ggml_gallocr_reserve_n's return, so a worker death BEFORE a sched re-reserve (e.g. first decode) dereferenced NULL buffers in gallocr_alloc_graph and killed the coordinator before any 29b containment could react (prior kill-tests always hit mid-decode with buffers already reserved); reserve failure now fails the decode cleanly. Kill-test matrix (0.6B, 2 loopback workers, CPU build): worker dies mid-serving -> 500 + in-process reload onto survivor + next request OK (same PID); dead worker RETURNS -> re-included on the next reload; BOTH workers dead -> loud local-only degrade, serving continues; no flag -> unchanged 500 + exit 42. Remaining refinement (filed, not blocking): surgical share re-split without a full reload (keep unaffected KV; only re-upload the lost share) - worthwhile once the dedicated-attention topology makes worker shares stateless expert weights. **BUILT (2026-07-14, bc5137276) - EXPERIMENTAL, opt-in LLAMA_RPC_SURGICAL=1.** Mechanism: a per-buffer client-side journal records weight-class writes (big: offset/size/HASH - replayed via SET_TENSOR_HASH against the restarted worker's local cache; small strided per-row writes: literal bytes spilled to an anonymous temp file, GGML_RPC_JOURNAL_MAX_MIB cap), plus buffer clears and quantized-pad inits; on the worker's return at the same endpoint the buffers are re-created, the journal replays (read-back hash-verified under GGML_RPC_REPROVISION_VERIFY=1), and the meta backend rebases its shadow registrations onto the new bases (buffer usage now propagates meta->members so compute members are exempt; meta member-alloc failure downgraded from abort to clean error - it fired as a coordinator SEGFAULT-class abort when a sched re-reserve hit a dead endpoint). Server side: --rpc-reload tries surgical first (waits LLAMA_RPC_SURGICAL_WAIT_S=120 for the endpoint to return), falls back to the full reload on ANY failure. MEASURED: recovery ~2s after the worker returns (7-8s including a 5s simulated restart) vs minutes for the reload; server, HTTP state and other members untouched. WHY STILL OPT-IN: on 6L-truncated V4 the post-surgical generation is self-consistent but deterministically DIFFERENT from the fresh state (wide-margin token flip, similar vocabulary) even though journaled regions verify byte-exact, clears are replayed and MIRRORED non-weight statics are sibling-restored; a no-kill control reproduces the fresh output, so the delta is real. Remaining suspects for next session: slot/prompt-state interaction with the failed in-flight request, non-mirrored graph-computed statics (deepseek4 hash-layer tables' classification), a rebase gap. PPL-equivalent VERDICT (2026-07-14, teacher-forced top-30 distributions over 22 positions via the server, identical prompts pre-kill vs post-surgical): top-1 agreement 11/22, mean |dlogprob| on shared tokens 1.81 nats (max 7.7) - a LARGE distributional shift, NOT a benign rounding-path change (those sit ~1e-3). The surgical restore corrupts or misses real state; stays opt-in. Next diagnostic: whole-buffer differential of the restored worker pre-kill vs post-surgical (GET both full buffers and diff regions against the journal map), plus classify deepseek4's graph-computed statics (hash-layer tables) - the harness lives in the session notes (workers sw1/sw2 :50181/:50182, 6L V4, port 18095). **RESOLVED (2026-07-14): the delta was a journal KEY COLLISION - fixed, gate now byte-exact.** `rpc_buffer_journal::sets` was keyed by the TENSOR-relative write offset; every tensor's first chunk is offset 0 (and the 32 MiB chunked uploads reuse the same offset series), so on a member buffer holding many shadow tensors the std::map last-write-wins dedup silently dropped all but the last tensor's entries per key - the replay restored only a sliver and left the rest of the restarted worker's share as fresh zero pages (attention is CUDA0-dedicated = intact; RPC1's expert share mostly zeroed = exactly the observed self-consistent-but-shifted output). The read-back verify passed because it only checks SURVIVING entries - it could never see a dropped one. Fix: key by buffer-relative start (root offset-from-base + write offset); a rewrite of the same region still dedups. Added tripwires: the restore log now prints journal COVERAGE (a weight buffer far below 100% = lost or journal-bypassing writes), a sibling-restore count, and a post-rebase stale-shadow scan. Verified (6L V4 harness): weights member buffer 5904 MiB restored as 163 cache-replayed + 122 literal writes, journal covers 100.0%, verify 285 regions 0 mismatches, sibling-restores 0 (dsv4/KV statics are CUDA0-dedicated, so nothing mirrored lives on worker non-weight buffers - as designed), 0 stale shadows; post-surgical output BYTE-IDENTICAL to the fresh baseline ("raeli kinain...") across 3 kill/restart cycles on 2 server instances (recovery ~14 s with the full read-back verify armed). Stays opt-in pending a fleet soak, but the known corruption is closed. FLEET SOAK KILL-TEST PASSED (2026-07-15, full 86.7GB V4, real .11 worker docker-restarted mid-serving): clean 500, surgical re-provision of the 48GB share from .11's local cache, TOTAL recovery 121s (vs ~10+min reload; cost = one worker's cache re-verify), post-recovery output byte-identical to baseline. Workers needed no changes - coordinator-side env only. (a) is cheap and immediately useful for fleets like ours where laptops come and go; (c) is a real project. Pairs with #26 (worker-local sourcing makes redistribution loads fast). **Plus (d) worker presence broadcasting — ✅ DONE (2026-07-10)**: `rpc-server -a/--announce` (+ `--announce-group ADDR:PORT`) multicasts a versioned text beacon every ~2 s (`llama.cpp-rpc/1 port=… proto=… devs=… free_mib=…`; coordinator derives the IP from the datagram source, TTL 1, egress pinned to the `-H` interface so exposure never widens); coordinator `--rpc-discover` (+ `--rpc-discover-group`, envs `LLAMA_ARG_RPC_DISCOVER[_GROUP]`) listens 2.5 s post-parse, registers every unique worker heard, warns-and-continues when none found, and always uses skip-unavailable semantics (a worker can vanish between beacon and connect). Plumbing: beacon+listener in `ggml-rpc/transport.{h,cpp}` (default group `239.255.77.99:50153`, listener joins on INADDR_ANY + loopback so same-host workers bound to 127.0.0.1 are heard), C API `ggml_backend_rpc_start_announcer`/`ggml_backend_rpc_discover` exposed via registry proc-addresses (DL-build-safe); `add_rpc_devices` gained a process-wide endpoint dedup (same worker in `--rpc` AND discovered → registered once; failed registrations un-tracked for retry). Verified (loopback CPU build, 0.6B): discovery→load→generate on the discovered worker; `--rpc` + `--rpc-discover` dedup (single RPC0); no-workers → clean WARN + local run; custom group both sides works; mismatched group finds nothing (correct). Real-LAN multicast (through the physical switch) still to confirm on the fleet. Docs: guide §1/§2, env-gates §7. Free-mem/devs already in the payload — the #31 speed-score extension has a natural home. Original design note: default stays exactly today's behavior (static `--rpc` list, no broadcast); trusted networks only. **Plus (e) worker error containment — ✅ DONE (2026-07-10 evening; the observed 1660 Ti crash itself turned out to be #32, fixed separately).** Design: CUDA errors are funneled through `ggml_cuda_error` (every `CUDA_CHECK`/`CUBLAS_CHECK`), which now **throws** `std::runtime_error` instead of aborting when `GGML_CUDA_ERROR_CONTAIN=1` — armed automatically by `rpc-server` at startup (explicit `=0` restores abort; coordinators/tools never arm it, zero default-behavior change; the unwind crosses C++ frames only). `rpc_server::graph_compute`/`graph_recompute` route through new `compute_contained()`: catches the exception (or a non-SUCCESS status — both sites previously `GGML_ASSERT`ed even on a *returned* failure), logs, **drops only that connection** (stored graph nulled so a failed graph is never RECOMPUTEd), and a consecutive-failure counter (reset on success) treats 3 strikes as a poisoned backend → `std::_Exit(1)` (no atexit/CUDA teardown that could hang) so `restart: always` resurrects a clean process. Coordinator side: `ggml_backend_rpc_graph_compute` send failures now return `GGML_STATUS_FAILED` (+ uid invalidation, never RECOMPUTE across a reconnect) instead of `RPC_STATUS_ASSERT`-aborting. Fault injection: `GGML_CUDA_INJECT_COMPUTE_FAIL` (`N>0` = fail Nth-and-after; `N<0` = fail only |N|th). **Verified (V100 loopback, injected CUDA errors): (i) isolated error — worker logs `failure 1/3`, drops the connection, STAYS UP, and a fresh coordinator session on the same process loads + generates normally (exit 0); (ii) poisoned backend — failures 1/3, 2/3, then clean self-exit on the 3rd; (iii) plain local-GPU run unaffected.** Known remaining gap (deliberate, = scope of (b)): the *coordinator* still aborts when the dropped connection surfaces at the next blocking RPC (set_tensor_hash/synchronize recv — void APIs, ~40 `RPC_STATUS_ASSERT` sites); restarting it is cheap against the worker's warm cache. Guide §6 + env-gates §7 updated.
- [ ] **28. Expert-parallel distributed workers (MoE experts as the distribution unit)** — 🔄 **increment 1 DONE (2026-07-11 midday): meta-over-RPC works.** `-sm tensor` + `--rpc` + `--device RPCx,RPCy` builds `Meta(RPC,RPC)`; GLM-4.7-Flash (MLA: attention mirrored, only experts/FFN split = the EP shape) runs coherently across .11+.15. Four blockers found+fixed (details plan doc §5.1): meta static shadow-arena retirement on exact-fill KV ctxs (pure-attention models crashed, hybrids never did); O(n^2) graph serialization — src closures walked past subgraph boundaries, measured **53 MB/token** on GLM → leaf-cut serialization; per-token graph re-serialization (single last_graph_uid never matches ~96 cycling subgraphs) → RPC proto **4.3**: `GRAPH_COMPUTE_UID`/`GRAPH_RECOMPUTE_UID` + per-(device,uid) server LRU cache, steady-state = 16 B/subgraph; per-row weight upload (12M SET_TENSOR calls + 4.3 GB descriptor overhead per GLM load, defeated --model-dir hashes) → RPC `set_tensor_2d` client-side row gather. New diagnostic: `GGML_RPC_STATS=1`. Gates: coherence PASS; wire PASS (mixed-fleet 2.9 MB/token, all-4.3 ≈ 1.5 MB/token — inside §2 band); PPL meta 8.1284 vs single-box 8.0294 (+1.2%; .11-only == .15-only exactly, and LLAMA_META_DUP_DEVICE=2 on ONE worker reproduces 8.1284 EXACTLY → delta = inherent 2-way split arithmetic, wire adds zero error — PASS). Perf trail: GLM fleet decode 0.7→4.2 t/s from leaf-cut alone (workers still 4.2-proto); loopback 0.6B 11.6→18.1. Worker images rolled twice more the same day; fleet now on `rpc-worker-cachefix`/`75-cachefix`. **Increment 2 (2026-07-11 evening): CAPACITY PROVEN — the 86.7 GB DeepSeek-V4-Flash (fits NOWHERE in the fleet singly) runs COHERENTLY RAM-resident across .11+.15+.25.** Perf gate NOT met yet: 0.4 t/s vs the 8.1 target (2-worker GbE-only config: 1.2-1.3) — attribution nailed: .25's 100-Mbit link sits in every reduce boundary (bandwidth law applies to LINKS: slow-link boxes need tiny/cold shares = true EP placement, increment 3), sequential fenced W2W pulls per boundary, per-token client subgraph rebuild. On the way, found+fixed TWO fleet-wide correctness/stability bugs that masqueraded as split bugs for a whole day: (1) the rpc-server tensor cache (`-c`) served UNVERIFIED entries — killed loads left truncated files that poisoned every later load (upstream has the same hole); now atomic tmp+rename writes + re-hash verify-on-read with self-healing fallback (`05a0634c3`); (2) cache-miss loads build model-sized dirty page cache on top of the weights and can OOM a tightly-sized worker; now fsync+fadvise per entry + clean load failure instead of coordinator abort (`c18e3dbbc`). Debug enablers: `LLAMA_TRUNC_ARR=1` + `--override-kv <arch>.block_count` (minutes-scale truncated reproducers), `GGML_META_NO_DELAY=1`, `GGML_RPC_STATS=1`. V4 split math verified clean (loopback dup-device token-identical to single-CPU). **Perf lever 1 landed (2026-07-12, RPC proto 4.4): batched W2W reduce pulls.** The allreduce fallback's pulls of one butterfly step now overlap on the wire: all fences captured before any COPY_FROM_REMOTE is sent (early fences are safe - they already cover the producing compute - and keeping sibling pulls out of each fence is what removes the serialization), then all requests, then the acks. Required a server fix: 4.3 held the per-process exec lock across the whole pull, so two crossing in-flight pulls ABBA-deadlocked ACROSS worker processes; 4.4 fetches without the lock and takes it only for the local write (same shape as GET_TENSOR_FENCED). Client batches only when both ends are >= 4.4 - mixed fleets keep the sequential path (validated vs a real 4.3 server: no deadlock/crash; old-dst-pulls-new-src degrades to the bridged copy until the roll). Gates: 0.6B loopback 18.4 t/s coherent; truncated-V4 meta token-identical to single-CPU. **Fleet A/B done (2026-07-13, fleet rolled to 4.4): V4 2-worker .11+.15 `-ts 3,2` reduce 40 -> 25.5-28.5 ms/graph piece (-35%); decode unchanged (0.98 timed / 1.20 untimed) because compute (155-223 ms/piece, ~85% of token time) dominates -> multi-build subgraph cache is now decisively the next lever.** **Perf lever 2 landed (2026-07-13, c20e432b4): multi-build subgraph cache - builds keyed by outer-graph content hash (sched pieces carry a fresh uid every token so uid equality never matched; every call redid the full client build, ~30ms/piece), shadow-ring rotation as validity horizon, init_tensor memo against arena growth. Fleet 2-worker V4 0.98 -> 1.16 timed / 1.20 -> 1.25 untimed, 128/128 hits; token-identical to previous commit on 0.6B + V4-trunc loopback. Remaining ~800ms/token is per-boundary worker compute (mirrored attention runs fully on every member) -> next levers: increment 3 placement, reduce/compute overlap.** **Increment 3 prototype landed (2026-07-13, fd5bccf38): LLAMA_META_ATTN_OWNER=<j> dedicates DSA attention weights+cache to one member via DEGENERATE splits (zero-size slices disable non-owner compute; dot-dim exit derives PARTIAL so the existing reduce broadcasts x+0=x); derivation gains degenerate-state escapes; zero-slice disable now FILLs 0 (SCALE*0 turned recycled NaN/Inf into poison). Validated: token-identical to mirrored on CPU loopback under NO_DELAY; Meta(CUDA0,RPC,RPC) coherent through 30-layer truncations. The depth corruption was FOUND+FIXED (7c1824615): the delayed-AllReduce matcher stepped past consumers without checking use count, so hyper-connection streams read UNREDUCED attn_out (zeros on non-owners) and members drifted per layer. **FULL V4 COHERENT on Meta(CUDA0=V100 attention owner, .11+.15 experts `-ts 0,3,2`): correctness milestone of increment 3 DONE.** Boundary optimizations (d23d9795a: single-computer reduces become broadcasts - no zeroing/butterfly; island-internal deg-x-deg matmuls stay owner-local, only attn_output_b exits broadcast) took boundaries 26.8 -> 21.5/graph and reduce 82-88 -> 40 ms/graph. **DECODE 1.80 t/s untimed - the EP design now beats the mirrored fleet (1.25) by 44%; single-box bar 3.54 is the next target. Computers-only moe reduce RESOLVED NEGATIVE (4e6e6c1b5: 1.34 vs 1.80 t/s - star hops through the local zeroed CUDA member beat W2W fenced pulls; full butterfly kept, note in code). Slice-aware sizing RESOLVED BY MEASUREMENT: the 67GB reserve was the DEFAULT ub=512 DSA worst case - at -ub 256 the reserve is 258 MiB and -c 4096 runs fine (prefill 7.55 t/s on a 203-token prompt, decode 1.82 t/s - best yet). Recommended config: -c 4096 -ub 256 -b 256. Uniform per-member allocation remains as virtual-memory hygiene debt only. .25 cold-share experiment RESOLVED NEGATIVE (2026-07-13): -ts 0,3,2,0.5 (9% experts on the 100-Mbit box) = 0.42 t/s vs 1.82 without it, coherent - the cost is ~16ms PER BOUNDARY (link latency x ~112 boundaries/token), not per byte of experts, so NO share is small enough: slow-LINK boxes must stay OUT of the synchronous decode ring entirely. Their capacity role needs an async mechanism (network-backed expert paging, batch-only participation) - increment-4 territory. Note V4 does not need .25 for capacity in the dedicated config (experts 80GB fit .11+.15 at 3:2). Remaining levers: dispatch pipelining, EP batching (inc 4).** **Perf lever 3 landed (2026-07-13): STAR REDUCE - the dispatch-pipelining lever.** With a local member (the CUDA attention owner) available as root, each multi-computer reduce boundary now does ONE batched read of every computing member's partial (new RPC reg proc `ggml_backend_get_tensor_batch`: all GET_TENSOR requests go out before any response is read, so the workers' compute tails and their transfers overlap; in-order serving makes each read an implicit fence on the producing compute), sums the partials on the host, and broadcasts identical result bytes with fire-and-forget SETs. The fold+butterfly's 2-3 sequential blocking round trips, CUDA ADD launches and worker-side ADD turnarounds per boundary are gone, and zero-share members stop paying FILL kernels. Client-only: no protocol bump, deployed worker images unaffected; all-wire fleets (no local root) keep the butterfly; `GGML_META_NO_STAR=1` restores it. Also fixed: batch procs were looked up only from member 0's reg, so Meta(CUDA0,RPC,...) never found cpy_batch. Gates: 12L-truncated V4 loopback Meta(CUDA0,RPC,RPC) token-IDENTICAL star vs butterfly (host f32 sum keeps the fold order; fp add is commutative), and the FULL 86.7GB V4 fleet outputs are byte-identical star vs butterfly. Fleet perf (CUDA0 owner + .11/.15 `-ts 0,3,2 -c 4096 -ub 256 -b 256`): decode **2.08 / 2.24 t/s** (two runs) vs same-binary butterfly controls 1.30/1.69 and the prior best 1.82; prefill 8.39/8.24 vs 7.55. Best fleet number yet; bar = 3.54 single-box. Loopback 12L truncation: 7.33 -> 8.84 t/s. **Attribution round 2 (2026-07-14, measured on the live fleet): the EP decode is LATENCY-dominated, not compute-dominated.** Facts: .11 runs ~1.0-1.6 cores of 16 during decode with all 8 pool threads engaged evenly at ~20% duty (per-thread /proc sampling), so worker expert math is only ~2.3 ms per layer (~100-130 ms/token); the coordinator main thread is recv-blocked ~2/3 of each token (wchan=wait_woken histogram); META_TIMING shows 2 meta pieces/token and only ~250 ms/token inside the meta backend. Reconstruction: token ~= 43 per-layer boundary cycles x ~7 ms (2.3 ms expert math + ~4-5 ms turnaround: worker cmd parse, uid cache lookup, threadpool wake/join, response send, RTT) + CUDA attention (~50-80 ms) + tail. CONSEQUENCES: (a) mirrored-middle-op placement and more worker threads are near-worthless at batch 1 (workers 80-93% idle); (b) remaining single-stream levers are per-cycle turnaround polish - worker-side spin threadpool (ggml_backend_cpu_set_threadpool with polling, workers have idle cores to burn), fewer syscalls per boundary - worth maybe 10-20%; (c) the ~3-4.5 t/s single-stream ceiling estimate for this fleet+model stands; batch remains the scaling axis. Image roll 2d50d2b42 regression-free: 1.93 t/s decode fresh-cache run (noise band 1.9-2.24). **Spin-threadpool RESOLVED NEGATIVE (2026-07-14, db6b42db6, fleet-measured): GGML_RPC_THREADPOOL_POLL attaches a persistent (optionally busy-polling) pool to worker CPU backends - without it every subgraph creates+joins a disposable pool. Loopback +2% and token-identical, but fleet poll=50: 1.97/1.97 t/s vs off-band 1.93-2.24 (median 2.08) - no gain, possibly a hair of loss. Thread create/wake is NOT the per-boundary turnaround; the cycle cost lives in the message sequencing (worker serve loop, per-command round trips). Feature stays env-gated OFF (harmless, may help other worker classes). The identified NEXT single-stream lever, if ever pursued: a fused boundary command (proto 4.5-class GRAPH_COMPUTE_RETURN: worker computes a subgraph and returns the boundary tensor in the SAME response; reduced value + next subgraph accepted in one message) - halves the per-boundary messages, est. 480 -> ~400 ms/token. Deeper redesign; not started.** **BUILT AND LANDED after all (2026-07-14, 9d735e3fb + 8ba6baee1, proto 4.5): the fused boundary command.** One GRAPH_FUSED message per wire member per boundary carries the reduced value (SET), the CHAIN of subgraphs up to and including the next reduce subgraph (uid list) and the request for that boundary's partial, answered in the response - and broadcast boundaries with a local sole computer distribute through the same path, so the fetch rides across them too. Loopback plumbing collapse: GET_TENSOR 1584 -> 72 and RECOMPUTE_UID 1764 -> 252 per run. Gates: PPL fused == unfused BIT-EQUAL (8 chunks, 12L truncation); byte-identity confirmed then found FLAKY on CUDA-owner configs (the same config self-differs run-to-run - gotcha #4 routing noise observed live; PPL is the gate). Fleet paired same-day A/B: fused 1.97 vs unfused 1.81 t/s (+9%); absolute numbers sat in the historical noise band, and late-day fleet measurements gained a confound: the production MTP server was recreated onto :latest mid-session and now holds both V100s. Pipeline enters at a piece's first star boundary, never spans graph calls, auto-off under GGML_META_TIMING / NO_STAR / pre-4.5 workers; GGML_META_NO_FUSED + GGML_META_FUSED_BCAST=0/1/2 for A/B. Fleet worker images rpc-worker-9d735e3fb / 75-9d735e3fb deployed. **FINAL fused A/B on proto-4.6 workers (85ec03b1f; chains re-gated on minor>=6 after the 4.5 format changed under it - mixed fleets degrade to unfused), clean box (prod serving stopped): fused decode 2.36 / 2.56 t/s (423/390 ms/token) vs paired unfused 1.72 - +37-49%, engagement 43,344 GRAPH_FUSED calls with plain GETs collapsed 22k -> 516 per run. NEW BEST fleet single-stream: 2.56 t/s (session arc: 1.82 -> 2.08/2.24 star -> 2.56 fused), 72% of the 3.54 single-box bar; prefill 8.4 -> ~4.1-4.5 on the 10-token probe is prompt-size noise, ub-256 prefill unchanged. The boundary cycle now costs roughly one round trip per layer plus CUDA attention.** **Increment 4 MEASURED (2026-07-13): EP batching scales - the #31 revisit is confirmed.** llama-batched-bench, full V4, fleet EP config (CUDA0 owner + .11/.15, npp 64 ntg 32): TG aggregate **1.66 / 2.71 / 3.82 / 4.73 t/s at B=1/2/4/8** (x2.85 at B=8, vs the #31 pipeline law's +27% - the EP composition law holds for batching too; boundary cost is paid once per step regardless of B). Aggregate crosses the 3.54 single-stream single-box bar at B=4. Coherence gate: llama-parallel -np 4 on the full fleet model - all four concurrent temp-0 responses fluent and on-topic (identical prompts gave identical responses). Multi-sequence dsv4 cache works on the EP config unmodified - no code needed for this increment; it is a property of the meta EP design. HONEST CAVEAT: the single box ALSO scales (2x V100 -ncmoe 99, same bench: 3.85/5.37/7.07/8.35, x2.17) and stays ahead in absolute V4 throughput at every B on this hardware - V4 half-fits in 46 GB RAM with NVMe paging behind it. EP's absolute win remains the capacity regime (a model/fleet where no single box holds the experts); its batching win is the better SCALING slope plus not burning the coordinator's RAM/NVMe. Slow-link async participation (.25 batch-only/expert-paging) stays PARKED as stretch - not needed for V4 capacity and the boundary-latency law rules it out of the sync ring. **New single-box baseline (2026-07-13): V4 on the coordinator, 2x V100 `-ngl 99` + `-ncmoe 99` (all routed experts in a CPU buffer, mmap-backed - 46 GB RAM holds ~half the ~80 GB of experts, rest pages from NVMe): decode 3.09 t/s cold -> 3.54 t/s warm (127-token run, page cache saturated at 41 GB), prefill 1.6 t/s. Slightly ABOVE the 2.7-3.1 ssd-streaming baseline - the best single-box V4 number on this hardware, and the number the fleet EP path has to beat.** — **phase 0 DONE (2026-07-11): `docs/expert-parallel-plan.md`** — design + projections from the fleet's measured numbers. Core: the composition law flips from #31's SUM-of-stages to **max-over-contacted-workers** (a slow box owning few/cold experts adds capacity while ~never gating a token — the "smallest computable chunk" principle, user-guided). Wire math: 8-16 KB activations out / 16-48 KB expert outputs back per contacted worker per MoE layer; latency floor ≈ layers × RTT ≈ 13 ms/token on this LAN. **The prize: DeepSeek-V4-Flash 81 GB becomes RAM-resident across the fleet's pooled 176 GB → projected 18-40 t/s vs today's 2.7-3.1 SSD-streamed — 7-15×.** Implementation route identified: the meta tensor-split backend ALREADY does intra-layer expert segmentation (AXIS_1 gate_up + mul_mat_id PARTIAL, validated on Qwen/GLM); increment 1 = run a meta device over RPC member devices (proto 4.2 gives async+events; the per-layer reduce is a k-vector sum, NOT a GEMM row-split reduce — #17's Ethernet-AllReduce verdict doesn't apply). Increments + gates in the plan doc: (1) meta-over-RPC spike w/ 35B experts on 2 workers, (2) the DeepSeek fleet run (gate: ≥3× SSD baseline), (3) bandwidth-weighted then hot/cold placement (routing stats), (4) EP batching (where MoE -np CAN scale, unlike #31's finding for pipelines). Original filing below. The expert is the most network-friendly compute unit in the stack: fully independent FFN per expert (zero cross-expert communication), merge = a weighted add of k small vectors, demand-driven (only k of 128-256 needed per token). Design sketch: workers own expert SUBSETS (per-layer or hashed); the coordinator runs attention/router locally and ships each token's activations only to the workers owning its routed experts; workers return their expert outputs for the weighted sum. Marries the two proven tracks — ssd-streaming's per-expert machinery (slices, id→slot routing) + the RPC worker fleet — and is how large-scale MoE serving distributes. Would let N cheap RAM boxes collectively hold a model none of them fits (e.g. 81 GB DeepSeek across 2×64 GB CPU workers) with per-token traffic ~k×(2×hidden×4B) ≈ tens of KB. Key property vs pipeline: expert calls run in PARALLEL across workers — adding boxes cuts per-token time instead of adding stages. Open questions: router locality (hot experts pinned locally), per-worker batching, latency. Big effort; distributed phase 5. **INCREMENT 3 CLOSED (2026-07-15): bandwidth-weighted EP placement is now AUTOMATED — `--rpc-auto-weight` supports `-sm tensor`: shares map to the meta device's members (plain registry order, the tensor-branch device list), score-proportional and capacity-capped (compute allowance only; no whole-layer margin, KV lives with the owner), `LLAMA_META_ATTN_OWNER=<j>` gets a hard 0%% share tagged "(attention owner)"; worker-CPU members keep their scores (RAM members are the point in EP; the #35f sibling rule stays layer-only), #31 law-1 warning suppressed (EP is the capacity play). Gates (loopback): 0.6B 2-member tensor mode 41.2/58.8 exactly score-proportional, output unchanged; trunc-V4 3-member with owner=0 -> 0%%/49.7%%/50.3%% (capacity cap engaged) and temp-0 output BYTE-IDENTICAL to the single-CPU reference; -sm layer path regression-unchanged (same KV+compute reserve line, same shares). Composes with #38 Include (a reload now re-splits EP fleets automatically) — drop COORD_TS and set COORD_AUTO_WEIGHT=1 in the EP scripts to use it. Hot/cold routing-stats placement is RESOLVED BY THE LATENCY LAW (attribution round 2: boundary round trips dominate; placement cannot reduce boundary count in the sync ring — the .25 cold-share negative already showed shares do not matter for slow LINKS), leaving async slow-link participation + EP batching as the parked increment-4 stretch.** **INCREMENT 4 CLOSED (2026-07-16): batched EP SERVING is now safe - a fleet-killer in multi-slot serving found and fixed.** `llama-server -np 4` over the EP topology was never actually validated (batched-bench and the llama-parallel coherence gate ran 2026-07-13, BEFORE the fused-boundary/build-cache stack landed 07-14) and crashed the attention-owner worker within 2-3 rounds of mixed 5-concurrent-request traffic: the kv-cell ids graph input (`Meta#leaf_14`) arrived as random 64-bit garbage and set_rows aborted the worker (`ops.cpp:5063 GGML_ASSERT(i1 >= 0 && i1 < ne1)`). ROOT CAUSE: the meta prepared-build cache keys on an outer-graph content hash that mixes `t->data` - but sched-allocated meta tensors carry LOGICAL offsets, identical across two same-shaped scheduler arenas, and the buffer identity was mixed only for NON-meta tensors; when a decode-graph cache entry was EVICTED (default 4 entries < np4's ubatch-shape diversity) and a same-shaped entry rebuilt, the build cache produced a FALSE HIT whose member subgraphs still referenced the dead entry's freed worker-side allocations. Bisect (trunc-V4 6L, 3 CPU loopback workers, owner=0, 12 mixed rounds x 5 concurrent): default = crash at round 2-3 (3/3 runs) - `GGML_META_MAX_GRAPHS=128` still crashes (NOT ring age) - `LLAMA_DECODE_GRAPH_CACHE=0` survives - `=32` survives (eviction-dependent, nails it). FIX: meta buffer contexts carry a never-recycled uid, mixed into the build key for meta-buffer tensors - an evicted+rebuilt arena now misses and rebuilds (steady-state hits unaffected: a live arena's uid is stable). Gates: the exact crashing config survives 12/12 rounds; decode-graph reuse intact (605-649 graphs reused/slot); Qwen3-0.6B dense 2-worker tensor-mode temp-0 BYTE-IDENTICAL to the no-RPC reference. `set_rows` now logs dst/src1 names + index/bounds before the abort (the canary for stale graph inputs). CAVEAT recorded: pre-fix, the same stale-build reuse could corrupt np>1 tensor-mode outputs SILENTLY where the garbage landed in a non-asserting input - treat any pre-2026-07-16 multi-slot tensor-mode serving as suspect. **Async slow-link participation CLOSED NEGATIVE by physics** (the other inc-4 half): network-backed expert paging at 100-Mbit = ~12.5 MB/s payload vs the local NVMe's measured 1.6-2.7 GB/s O_DIRECT - 130-216x slower per byte, on top of the already-measured boundary-latency law (.25 cold-share: 0.42 vs 1.82 t/s) - and batch-only participation is structurally impossible without duplicating expert weights (a box that owns experts is in the ring for every token that routes to them). Remaining (not code, needs a fleet window + the fixed image): live -np 2/4/8 serving A/B to put real numbers behind the batched-bench slope; the EP compose keeps `-np 1` default until then.
- [x] **22. Audit all Dockerfiles + compose files for rot** — ✅ **DONE (2026-07-10).** Rot found + fixed across the session: (a) `cpu.Dockerfile` + `vulkan.Dockerfile` + `intel.Dockerfile` lacked `-DGGML_RPC=ON` → no worker binary; all three gained the flag + a lean `rpc-worker` target. (b) Both rpc-worker composes mounted `/cache` without `LLAMA_CACHE=/cache` → weight cache died with the container (full model re-stream per restart); fixed. (c) `cuda.Dockerfile`'s `CUDA_DOCKER_ARCH` ARG was DEAD (exported into `CMAKE_ARGS`, which cmake never reads, while the flag hard-coded 70); now wired — enables sm75+ worker builds. (d) Two `GGML_BACKEND_DL=ON` linker breaks (ssd-stream `dlsym`, rpc-server registry lookup) had made every DL-based image unbuildable. (e) Obsolete `version:` keys stripped from 5 composes (all 8 now `config -q` clean). (f) `docker-compose-glm-4.7.nospec.yml` still pointed at the disliked REAP model with a Qwen copy-paste header → rewritten for GLM-4.7-Flash-UD, single-GPU, full 202K native ctx. (g) Stale/dangerous comments updated: mtp header now carries the server-verified numbers (27B ~81, 35B ~166 / 263 @ np4); `docker-compose.yml`'s obsolete "do NOT set p-min>0" and "-np>1 degrades" advice (both long since fixed) replaced with a supersession note pointing to mtp.yml. Out of scope: the non-Intel vendor dockerfiles we don't ship (musa/rocm/s390x/zendnn/cann/openvino) — left upstream-as-is.
- [ ] **12. Distributed inference implementation** — 🔄 in progress (design: `docs/distributed-inference-plan.md`).
  - ✅ **Phase 0 measured**: loopback RPC pipeline (GPU0 local + GPU1 via `ggml-rpc-server`, `-sm layer`) = **32.3 t/s vs 33.2 in-process** — the synchronous RPC hop costs ~3% (~0.8ms/token); coherent output. Pipeline-over-RPC is confirmed practical for a future second box.
  - ✅ **Heterogeneous 2-worker fleet measured (2026-07-10)** — worker A i7-9750H (64 GB), worker B Core Ultra 9 285H (64 GB, Arc 140T). 35B whole-model: A **7.4**, B **10.4**, B-iGPU-Vulkan **10.0** (decode parity with B's CPU — same LPDDR5X, bandwidth-bound — but prefill 2× worse, ANV lacks XMX → tentative #27). **2-worker `-sm layer` even split: 7.4 = the slower box's pace** (default split is memory-proportional and both report 64 GB) — single-stream pipeline time is the sum of stages, so best-single-box wins when the model fits; pipeline = capacity/throughput tool. **W2W vs bridged (`GGML_RPC_NO_W2W=1`): 7.4 vs 7.8, indistinguishable at CPU token costs** (`pipeline parallelism enabled` confirmed engaged). 0.6B: 2-worker 17.9 vs 1-worker 19.9 (small models feel the hops). Guide §3b documents all of it. Remaining when hardware allows: the GTX 1660 Ti CUDA75 worker (dedicated-bandwidth test), `-np` throughput through the pipeline.
  - ✅ **FIRST REAL CROSS-HOST INFERENCE (2026-07-09, worker at 10.5.5.15, 0.15 ms RTT):** whole-model-remote on the CPU-image worker — 0.6B **19.9-20.1 t/s** (loopback 20.9, **~4% network tax**); 35B-A3B **7.4-7.6 t/s** (loopback 7.7, ~2-4%). The 23 GB cold stream took ~178 s (~130 MB/s, GbE-class saturation); warm reload hits the worker's `/cache` (the LLAMA_CACHE fix) — decode identical cold vs warm, transfer is purely load-time. Guide §0 documents the data-movement lifecycle with a diagram + these numbers.
  - ✅ **CPU-worker domain validated (2026-07-09, loopback)** — prep for the user's GPU-less worker box. Whole-model-remote on a CPU rpc-server: Qwen3-0.6B **17.8 t/s** (local CPU 16.5), Qwen3.6-35B-A3B **7.7 t/s** (local CPU 6.5) — **the protocol hop is free at CPU speeds**; output coherent. Worker threads: rpc-server's half-the-cores default BEAT all-cores on loopback (7.7 vs 7.0 — oversubscription); re-sweep on a dedicated box. Deliverables: `.devops/cpu.Dockerfile` gained `-DGGML_RPC=ON` + an `rpc-worker` target (build on the worker box, no CUDA); `docker-compose.rpc-worker-cpu.yml`; guide §1b (CPU-only worker). **Fixed a latent cache bug in BOTH worker composes**: `./cache-rpc:/cache` was mounted but `LLAMA_CACHE=/cache` never set, so the weight cache silently died with the container (=> full re-stream of the model every restart). Next (needs the real box): cross-network measurement (RTT tax), thread sweep, then the tail-layer RAM-extension scenario (`-sm layer -ts …`).
  - ✅ **Phase 4 slice**: `GGML_RPC=ON` + `ggml-rpc-server` shipped in the CUDA image; `docker-compose.rpc-worker.yml` reference profile (security notes included).
  - ✅ **Phase 1 worker side + protocol**: `RPC_CMD_SET_SPLIT_STATES` (proto 4.1) with name-keyed split-state store in `ggml-rpc.cpp`; client API `ggml_backend_rpc_set_split_states()`; worker lookup `ggml_backend_rpc_split_state_lookup()`; `ggml-rpc-server --tensor-parallel` wraps local GPUs in one meta device (verified: 2 GPUs exposed as one `Meta()` device, unknown tensors default mirrored).
  - 🔄 **Phase 1 coordinator side — implemented, one blocker left**: island detection (meta device description now `Meta[N](...)`, forwarded over new `RPC_CMD_GET_DEVICE_DESC`, client stores `desc @ endpoint`); `llama_rpc_push_split_states()` (llama-model.cpp, exported in llama-model.h) resolves RPC entry points via registry proc-address, computes states with synthetic userdata `{island_n_devices, model}`, pushes before allocation — hooked in `load_tensors` (weights) and `llama-kv-cache.cpp` ctor (cache). **Verified E2E: coordinator logs "uploading 310 split states to TP island (2 devices)"**. Mirrored-fallback also verified: without states the island still generates correctly (218 t/s, weights duplicated).
    **Blocker chain, 3 of 4 fixed** (repro: worker `ggml-rpc-server -tp -p 5006x`, coordinator `CUDA_VISIBLE_DEVICES="" llama-server -m /models/Qwen3-0.6B-BF16.gguf --rpc 127.0.0.1:5006x -ngl 99`):
    1. ✅ shadow keying: compute-ring entries carry an identity fingerprint (type/shape/op/params/data/view_offs/name — mutable fields like `buffer`/`flags`/src pointers deliberately excluded); mismatch = miss, recreated on demand (slice placement derives from `tensor->data`, valid for sched-allocated buffers). Static-container entries keep upstream blind pointer trust: they are only registered in-process where addresses are stable, and their per-device placement does NOT follow the recreation formula (a full-struct snapshot guard there falsely invalidated weights whose `buffer` field is assigned after registration → CUDA invalid-argument at load; fixed). All buffer/backend set/get paths route through the self-healing accessor. In-process regression gate: Qwen temp-0 byte-identical.
    2. ✅ graph derivation resolved weight leaves as MIRRORED: buffer USAGE never crossed RPC, so the worker's compute buffer read as non-COMPUTE and graph intermediates took the static-callback branch (name miss → mirrored). Fixed by implementing the upstream FIXME: optional `set_usage` hook in `ggml_backend_buffer_i` (last field, aggregate-init compatible), called from `ggml_backend_buffer_set_usage`; RPC client mirrors usage via new `RPC_CMD_BUFFER_SET_USAGE`.
    3. ✅ diagnosis tooling: `GGML_META_DEBUG=2` now also prints per-src resolution (`SRC_RESOLVE`); the mul_mat fallthrough abort names the node and src states.
    4. ✅ **TP island E2E WORKS** (Qwen3-0.6B: coherent output, 148 t/s, split weights + KV across both island GPUs). Root cause was not graph lifetime but `rpc_server::get_tensor` on results: each RPC command deserializes a fresh struct whose `src[]` are meaningless, and reading e.g. logits re-derived the split state through those srcs. Fix: shadow entries store their split state at registration plus a `by_data` identity index; `adopt_identity()` aliases identity-matched structs and seeds the split-state cache so no path ever walks deserialized srcs. Also: graph node shadows force-refreshed once per node per build (`ensure(..., force)`) since INIT_TENSOR-time registrations lack src links. In-process gate: byte-identical.
       **27B island benchmark (loopback)**: tg 33.3 t/s, coherent temp-0 output; all three push sites fire (865 weight + 64 KV + 96 recurrent-state uploads). Two more RPC fixes were needed for the hybrid arch: null-safe view_src->buffer checks (coordinator-side CPU s_copy views deserialize with null buffers) and the split-state push in `llama-memory-recurrent.cpp` (recurrent R/S cache allocates separately from the attention KV cache). References: 47.8 in-process tensor, 32.3 RPC pipeline. Follow-ups: (i) tg gap vs in-process is the synchronous per-token RPC chain (inputs SET + logits GET each round-trip; phase 2 async is the designed remedy) - note the island-as-remote-whole-model case is pessimal, the real topology only offloads a layer slab; (ii) ✅ VRAM audit done: zero mirrored fallbacks; the waste was allocation geometry - per-device buffers spanned the full linear space. Fixed: meta buft get_alloc_size returns per-device slice sizes, RPC client always queries islands for sizes, server-side sanitization treats meta tensors as logical addresses. Island VRAM 19.6 -> 12.1 GB/GPU (load path verified; final generation E2E interrupted mid-run - one smoke test pending). Smoke follow-through (2026-07-07): the interrupted run's crash (`get_tensor` bounds reject during prompt-checkpoint save) is fixed by the sanitization exemptions at HEAD, but exposed a worse silent bug: state save/restore (slot API, prompt checkpoints) reads KV via per-stream *view* structs, and view links don't survive RPC - the worker derived MIRRORED for the bare struct, reading garbage and stomping packed slices on restore. Fixed: RPC client resolves views to their root tensor with translated offset before serializing (`rpc_resolve_view`); meta set/get_tensor aborts loudly on unresolvable aliases of registered static entries instead of corrupting. Verified on 0.6B island: save byte-identical to in-process TP save, restore round-trip byte-identical generation; in-process TP gen/save/restore byte-identical to pre-change. (Note: TP saves differ from single-GPU saves by ~40% of bytes - low-mantissa allreduce noise, not layout; round-trips fine either way.) 27B island E2E at HEAD: ✅ COMPLETE - loads from worker cache in 40-50s, **9.2 GB/GPU** (slice packing landed even better than the 12.1 first measured), tg 32-33 t/s short ctx / 29.2 at 5.2k ctx, coherent output; 5212-token prefill created a real hybrid context checkpoint (149.6 MiB state_seq_get_data over RPC - the exact former crash site) cleanly; explicit slot save/restore of a 501 MB / 5259-token state round-trips with byte-identical post-restore generation. Item (ii) closed.; (iii) first-load streaming is ~40 min - worker `-c` cache makes it one-time.
       Remaining polish: (a) ✅ adopted-alias lifetime hardened (2026-07-07): ring-sourced aliases now register into the source entry's own slot so alias and shadows die in the same ggml_reset (ring lookups are fingerprint-checked, so address recycling stays safe); static-sourced aliases stay in the current ring slot — their shadows are immortal and the static container's pointer lookup skips fingerprint checks, so aliases must never live there. Verified: island save bytes unchanged, 12 shape-varied gens (> ring depth 8) + post-rotation restore round-trip byte-identical, in-process TP gen byte-identical. (b) ✅ done (see 27B island E2E above); (c) ✅ done (abort no longer derefs src names).
  - 🔄 **Phase 2 (async RPC) — infrastructure landed (proto 4.2, 2026-07-07)**: `RPC_CMD_PING` completion markers exploit strict per-connection ordering (no server state); client tracks ping sequence numbers per socket, blocking commands drain outstanding pings first; RPC backend now implements `set_tensor_async`, real `synchronize` (marker round trip), and full events (record = deferred ping, wait = drain; same-endpoint waits are free by ordering) — device caps report async+events, so llama's **pipeline parallelism gate now engages** for multi-RPC-device layer splits ("pipeline parallelism enabled", n_copies=4). Verified: 2-worker loopback pipeline (0.6B, one worker per GPU) byte-identical to single-GPU reference, ON==OFF outputs, workers clean. Measured: no throughput change at this scale (tg ~198 both; pp 1957-tok ~6100 t/s @ub2048, ~490 t/s @ub256 ON==OFF) — profiling shows the serializer is the **cross-stage activation copy**, which the sched bridges through the coordinator as a blocking `get_tensor` (stage-0 wait) before the stage-1 set, defeating ubatch overlap regardless of n_copies. Next lever: the bridged copy. A client-side "deferred GET → SET on arrival" thread cannot preserve command ordering on the destination socket (the SET must precede the next GRAPH_COMPUTE, which the client thread sends immediately), so the correct design is **worker-side pull**: new `RPC_CMD_COPY_FROM_REMOTE {endpoint, src, dst}` sent to the destination worker (send-only, ordered naturally in its stream); the worker fetches from the source worker directly. Prerequisite ✅ done (2026-07-07): rpc-server is now multi-connection — thread per connection, one shared rpc_server (buffer registry + stored graphs), an exec mutex serializing command execution (idle recv waits happen unlocked; the lock is exactly the cross-connection dependency a pull's GET needs to wait out an in-flight compute). Per-connection buffer ownership preserves the old reclaim semantics (verified: killing a coordinator frees its buffers, 1709→361 MiB, worker keeps serving); stored graphs carry an owner connection and RECOMPUTE from another connection fails loudly instead of running the wrong graph. Verified: generation byte-identical with concurrent probe connections churning. ✅ **Fenced worker-to-worker pull landed (2026-07-07)**: `GET_CONN_ID` + per-connection executed-command counters (client sent-counter kept in lockstep, HELLO included) + `GET_TENSOR_FENCED` (source side: cv wait WITHOUT the exec lock, 10-min ceiling, wakes on connection death) + `COPY_FROM_REMOTE` (dest side pulls as a client over a cached persistent peer socket; soft-fails to the bridged copy) + client `cpy_tensor_async` for RPC→RPC across endpoints, version-gated (4.2 coordinator + 4.1 worker = features off; 4.1 coordinator refuses 4.2 workers). RAW hazard = the fence; WAR hazard = the blocking COPY ack (coordinator queues no further source work until the read is out — the CUDA cross-device event contract). Kill switch `GGML_RPC_NO_W2W=1`. Bug found on the way: the client async-state registry is keyed by raw socket address, and a recycled allocation inherited a dead connection's counters — every fence hung; fixed by resetting state on socket creation. Verified (2 workers sharing GPU1, CPU coordinator, `-sm layer`): bridged and W2W outputs byte-identical to the single-GPU reference and to each other; persistent peer connection confirmed; island save/restore unaffected. Loopback perf parity as expected — the win (1 direct hop vs 2 coordinator hops per handoff) needs real two-box hardware to measure. Usage notes documented in `docs/distributed-inference-guide.md` §5. Topology note (measured 2026-07-07, 0.6B, 1 local + 1 RPC stage, ub256): upstream's remote-stages-first default order is right — forcing the local stage first via `--device` measured ~33% SLOWER prefill (4080 vs ~6000 t/s); keep the default. TCP_NODELAY: already set on both sides (transport.cpp), audit done. ✅ Fixed: coordinator now fails fast with a clear error when an RPC server is unreachable (was: silent CPU fallback). Phase 3 (cross-host NCCL) still parked unless RDMA/25GbE exists.
- [x] **15. `--ssd-streaming` — three-tier weight placement (VRAM / RAM / SSD)** — ✅ **SHIPPED (beta); all follow-ups closed 2026-07-09.** Increments 1-3 done (streamed O_DIRECT buffer + batching; SLRU measured no-win → keep LRU; GPU landing byte-exact, Qwen 2.5→9 t/s). **The five "nails" all closed this session, PPL-neutral throughout:** `input_cpy` VRAM reclaim (§8.2.7, −82%/−61% compute buffer), parallel miss-path reads (§8.2.8, prefill +31%/decode-neutral), multi-GPU crash → GPU landing made cleanly single-GPU-only (§8.2.9, `-sm layer` exit 139→0), non-fatal gallocr OOM fixed (§8.2.10). **DeepSeek-V4-Flash 81 GB runs on ONE 32 GB V100 + RAM: ~71% GPU-cache hit, ~2.7 t/s (IO-bound).** CLI flags + env gates + `docker-compose.ssd.yml` showcase + `docs/env-gates.md` + `docs/ssd-streaming-plan.md §8`. **Residual known-limitations (deliberately parked):** DeepSeek `-sm tensor` load crash (meta-backend MLA split-policy, no-benefit config); full multi-GPU GPU landing (per-(class,device) pool keying scaffolded; low priority — 2-GPU streaming has no single-stream speedup over 1-GPU landing).
  - **Increment 3 (GPU landing, 2026-07-09, gated `LLAMA_SSD_STREAM_GPU=1`):** persistent VRAM expert-slot cache with id->slot indirection (ds4/#20757 design). Hook = the `compute_splits` expert-copy block (same code the inc-2 fix touched): a bounded K-slot VRAM buffer per expert-slice class; per token, touch the pool (hit=zero copy; miss=RAM arena L2 -> H2D to slot), remap ids->slots into a GPU scratch, alias `input_cpy` to the slot buffer, swap `src[2]`, skip the full copy. Re-entry handled by capturing the router-ids tensor per node. **Byte-exact vs resident (Qwen `7e35...`); Qwen decode 2.5 -> 7.0 t/s (~3x), still warming.** DeepSeek-81GB (77.9GB experts / 8-16GB cache, LRU ~30% hit) NOT winning yet (1.5-1.8 vs CPU 2.5) - needs 3.2e: reclaim the wasted full-size `input_cpy` VRAM, SLRU+2nd-miss admission (inc-2 policy, #20757 +8-15pp), longer warm; and 3.2b-3 (scope offload to streamed experts so decode uses it without global `GGML_OP_OFFLOAD_MIN_BATCH`). **Beta polish (2026-07-09): auto-offload streamed experts (Qwen 7->9 t/s, no global env), auto-size VRAM pools by class count, CLI flags `--ssd-streaming`/`--ssd-stream-gpu`/`--ssd-stream-budget`/`--ssd-stream-vram-budget`; `docs/env-gates.md` reference.** **Multi-GPU benchmarked (8.3): GPU landing is SINGLE-GPU-ONLY** (no-op under `-sm tensor` (meta backend) and `-sm layer` (class-keyed pool != per-device)); 2-GPU streaming works (layer-split, coherent) but no speedup over 1 GPU; resident tensor-split is the 2-GPU win (Qwen 108 t/s); DeepSeek `-sm tensor` crashes at load (meta split-state assert), layer-split runs (1.5 t/s). Follow-ups (**closed 2026-07-09, §8.2.7-8.2.10**, except the `-sm tensor` load crash which is a parked known-limitation): multi-GPU GPU landing (per-(class,device) pools + single-GPU gate), the tensor-mode load crash, prefetch/overlap. Detail: `docs/ssd-streaming-plan.md §8.2`. Run models whose weights don't fit in VRAM (or even VRAM+RAM) by streaming expert tensors from SSD on demand, as a third managed placement tier that composes with the existing two. **Working today**: DeepSeek-V4-Flash **81 GB runs on ONE V100 (32 GB) + 46 GB RAM** — load 9.2 s, coherent, decode **1.66 t/s** (30 GB expert cache; trajectory 0.69 cold → 1.00 warm → 1.29 batched → 1.66 O_DIRECT). Byte-identical to resident (Qwen-35B-A3B gate). Enabled by `LLAMA_SSD_STREAM_BUFFER=1` (+ `LLAMA_SSD_STREAM_BUDGET` MiB, `LLAMA_SSD_STREAM_SERIAL=1` for -ngl 0, `GGML_SSD_STREAM_DEBUG=1`). **Remaining (as of this snapshot — ALL now done, see the §8.2.7-8.2.10 sub-bullets + the item-15 header above)**: GPU landing (increment 3 — compute experts on the V100 via a bounded VRAM slot cache + `MUL_MAT_ID` id→slot indirection), then prefetch/overlap and the `--ssd-streaming` CLI flag. Full detail + increment staircase in `docs/ssd-streaming-plan.md §8`.
  - **Increment 2 (2026-07-08, `ggml-ssd-stream.{cpp,h}`): SLRU expert cache — MEASURED, NEGATIVE, keep LRU default.** Added an optional segmented LRU (probation+protected, 2nd-hit promotion, evict probation-tail-first) behind `LLAMA_SSD_STREAM_SLRU=1` (`LLAMA_SSD_STREAM_PROTECTED_PCT`, default 80). **DeepSeek 81 GB, 1×V100, `-ngl 99` decode hit rate (deterministic): 8 GB 54.0% (LRU) vs 52.9% (SLRU) · 20 GB 68.1 vs 67.8 · 30 GB 73.4 vs 73.6.** SLRU ties at 30 GB, marginally worse at 8/20 GB — routed-expert reuse isn't skewed enough for scan resistance to pay for itself; **LRU stays default (simpler, >= SLRU everywhere), SLRU kept opt-in.** Hit rates *beat* the feasibility projections (44% @ 34 GB); 30 GB → 73% hit → **3.1 t/s** (up from the 1.66 colder figure). Output-neutral (SLRU==LRU==big-budget byte-identical on the deterministic Qwen `-ngl 0` path). Also landed a `LLAMA_SSD_STREAM_NO_ODIRECT=1` buffered-read kill-switch and a hit-rate% debug line.
  - **Multi-GPU streaming gallocr OOM — DONE (2026-07-09).** The non-fatal `graph_reserve` OOM (worst-case ~73 GB compute buffer on one device under `-sm layer` streaming; failed + recovered) was the streamed-expert offload: host expert weights offloaded to a GPU build full-size `input_cpy` copies that gallocr's 512-token worst case sums; single-GPU shrinks them (reclaim), multi-GPU can't (shrink gated off). Confirmed streaming-specific (resident `-sm layer` = 100 t/s, no OOM). Fix: skip the streamed-expert offload when `n_gpu > 1` (keep them CPU-tier — correct, since GPU landing is single-GPU-only), removing the inflated reserve at the source; single-GPU untouched. Verified: `-sm layer` OOM lines 2→0, coherent (7.8 t/s); single-GPU PPL unchanged. Detail: `docs/ssd-streaming-plan.md §8.2.10`.
  - **Multi-GPU crash fix — DONE (2026-07-09): GPU landing is cleanly single-GPU-only.** GPU landing *engaged* under `-sm layer` (the auto-offload routes streamed experts through per-device CUDA backends) and **crashed** — `GGML_ASSERT(id >= 0 && id < n_expert)` (garbage ids from per-node orig_ids across 2 backends) + a class-only pool key aliasing a device-1 `input_cpy` onto device-0 slots. Fix: (1) pool key gained the backend → one pool per (class, device); (2) a single-GPU gate `ggml_backend_sched_n_gpu(sched)==1` guards the shrink + ssd_gpu detection + pool alloc + bind consistently, so `-sm layer` stays a clean CPU-tier fallback (and the reclaim shrink only fires where bind handles the node). Verified: `-sm layer` exit 139→0 coherent (pools=0, 3.5 t/s); single-GPU unchanged (73.69 MiB, **PPL 7.0898==baseline**); `-sm tensor` exit 0 no abort. Full multi-GPU landing left for later (2-GPU streaming has no single-stream speedup over 1-GPU landing). Detail: `docs/ssd-streaming-plan.md §8.2.9`.
  - **3.3 parallel miss-path reads — DONE (2026-07-09): prefill win, decode-neutral.** Env-gated profiling (`GGML_SSD_STREAM_DEBUG`) sized the GPU-landing miss path on DeepSeek-81GB: **read 26 s vs H2D-issue 16 s**, and **62% of VRAM misses also miss RAM** → a single-threaded O_DIRECT pread (~1.5 GB/s) dominated. Parallelized the preads (`LLAMA_SSD_STREAM_READ_THREADS`, default 1 = unchanged): `gpu_bind` now (1) touches pool + reserves RAM slot serially (`ssd_touch` gained a `defer_read` mode — SLRU isn't thread-safe), (2) preads RAM-missed slices across N workers each with its own O_DIRECT bounce buffer, (3) H2Ds every miss. **Result (96 tok, sweep 1/2/4/8): read 25.9→20.1 s (−22%, saturates at 2); prefill +31% (1.3→1.7 t/s); decode neutral (2.5→2.6 t/s, noise)** — decode touches ~6 experts/layer so within-call parallelism has little to chew on, and the token is co-bound by the untouched pageable H2D. Correctness: DeepSeek PPL rt=1 4.5240 == rt=4 4.5240. Verdict: helps the batch-amortized path (prefill/long prompts), not single-stream decode of a model this far over VRAM; kept env-gated. Further decode gains need H2D overlap+pin (Volta side-stream, risky per task 17) — not pursued. Detail: `docs/ssd-streaming-plan.md §8.2.8`.
  - **3.2e-a input_cpy VRAM reclaim — DONE (2026-07-09).** Reclaimed the dead-weight VRAM gallocr reserves for each streamed-expert `MUL_MAT_ID` input copy: `gpu_bind` redirects `input_cpy->data` to the persistent slot pool, so the full n_expert-slice copy is never read. New `ggml_ssd_stream_gpu_shrink_copy()` shrinks the copy to 1 slice at the copy-creation site in `ggml_backend_sched_split_graph` before gallocr sizes the arena; compute is unchanged (bind already overwrote ne[2]/nb[2]/data; residual nb[3] unused since ne[3]=1). **Correctness gate: PPL identical** (Qwen streamed+landing, 16 chunks: ON 7.0898 == OFF 7.0898; greedy temp-0 text is not usable — `-ngl 99` GPU-MoE near-tie noise flips output independent of the flag). **Reclaim (CUDA0 compute buffer): Qwen 407.00→73.69 MiB (−82%), DeepSeek-81GB 1344.25→517.77 MiB (−61%, ~826 MiB)** — additive cache headroom, removes the exit=137 pressure; hit/t·s identical at equal budget. Safety: shrink does NOT allocate the pool (runs pre-load when the registry is empty → would mis-size pools by class count, a bug I hit and fixed: 4×6.29 GB instead of 1.57 GB); pool allocated on the compute path, fallback aborts loudly if a shrunk copy is ever unhandled (catastrophic-OOM only). Kill-switch `LLAMA_SSD_STREAM_GPU_NO_RECLAIM=1`. Detail: `docs/ssd-streaming-plan.md §8.2.7`.
  - **⚠ Found + FIXED (2026-07-09) a pre-existing streaming correctness bug while probing (NOT increment 2 — its code is output-neutral: SLRU==LRU==no-evict byte-identical).** `llama-perplexity`/all-position eval on a streamed model gave GARBAGE (PPL 158-193 vs ~7 resident) though single-stream generation was coherent. **Root cause: the scheduler's "copy only the used experts" optimization** (`ggml-backend.cpp` compute_splits ~1582-1665) materializes a `MUL_MAT_ID`'s experts from the source buffer (`input->data`) during the input-copy phase — BEFORE any node in the split runs, so before the lazy eval-callback fill — copying unfilled (`MAP_NORESERVE` zero) arena pages. The `-cmoe` control (experts on a NORMAL CPU buffer, same partial offload) scored correct 6.20, which + reading the scheduler (antirez/ds4 study pointed at all-position being special) located it. **Fix: `ggml_ssd_stream_prefill_experts()` hook called from that block to make the used experts resident before the copy; no-op when streaming off.** Verified: Qwen-35B-A3B streamed PPL 158 -> **6.2022, byte-identical to `-cmoe` and resident**; flagship **DeepSeek-V4-Flash 81GB on ONE V100 -> sane PPL 4.0568** (was 100+ garbage). Same bug also caused the generation near-tie flip (first-use reads unfilled arena); that flip persists at `-ngl 0` SERIAL (5541 vs 7e35) but is now **proven quality-neutral** (PPL identical). Caveat: large prefill ubatches need budget >= one ubatch's expert union. `-np>1` concurrent streamed serving verified (2-slot server, 2 concurrent temp-0 requests coherent). Detail in `docs/ssd-streaming-plan.md §8` item 2.
  - **UX / CLI semantics (to finalize in design):** three combinable tiers — resident-GPU (`-ngl N`), resident-CPU (today's host layers), and streamed-SSD (new). `--ssd-streaming` alone = stream everything (min-memory mode); `-ngl N --ssd-streaming` = N hot layers resident in VRAM, rest streamed; all three = resident GPU slab + resident CPU slab + streamed tail. Likely knobs: `--ssd-stream-budget <MiB>` (pinned staging-ring size), placement policy (which layers get which tier — front-loaded resident vs access-frequency), and a per-tensor override story consistent with `-ot`/`--override-tensor`.
  - **Physics / honest expectations (measured on this box 2026-07-07):** the models NVMe sustains **1.6 GB/s** O_DIRECT sequential reads (`dd iflag=direct`, 4 GiB from a resident GGUF). A dense 27B Q4 (16.4 GB) fully streamed touches all bytes every token → **~0.1 t/s decode ceiling**; this is a *runnability* feature, not a speed feature, for dense decode. The economically interesting cases: (a) **prefill/batch amortization** — one layer stream serves the whole ubatch, so a 4096-token prefill pays the 16 GB stream once (~10 s) across thousands of tokens; (b) **MoE expert streaming** — only route-active experts per token, plus a hot-expert LRU cache in RAM/VRAM (GLM-class A3B: ~3B active of ~30B total → ~10x fewer bytes/token than dense); (c) **mostly-resident mixes** — e.g. 2 streamed layers out of 48 adds only ~0.4 GB/token ≈ +0.25 s/token.
  - ✅ **Phase 0 done (2026-07-07)** — `docs/ssd-streaming-plan.md`: measured baselines (CPU-resident 1.51 t/s vs mmap-thrash 0.21 t/s under a 14 GB cgroup cap with cold cache — the number to beat; caps without page-cache eviction are invalid, fadvise DONTNEED first), prior-art survey (upstream has no managed streaming per discussion #19163; issue #20757's expert-cache PoC = 12-14 t/s GPT-OSS-120B on 8 GB GPU validates the MoE sweet spot), and full code map: the tree already ships `--direct-io` O_DIRECT aligned reads, per-tensor file offsets (`weights_map`), an async pinned-staging upload pipeline, the RPC buffer as the non-resident-buft template, and the decode-graph cache for per-graph prefetch schedules. Gap: `-ot` only offers device-default bufts.
  - ✅ **Phase 1 done — NEGATIVE RESULT (2026-07-07)**: mmap residency director implemented (`common/ssd-streaming.cpp`, `LLAMA_SSD_STREAMING=1`, `LLAMA_SSD_STREAM_BUDGET=<MiB>`; byte-identical by construction, verified). 27B @ 14 GB cap, cold cache: thrash 0.266 t/s vs director 0.274 t/s — **no win**. Dense access is sequential and kernel readahead already overlaps IO with compute near-optimally (~3.7 s/token vs ~10 s serial floor); the disk is bandwidth-saturated, steering can't add bandwidth. Director stays as env-gated scaffolding (its ring/cursor/callback machinery is what phase 4 needs). **Redirect: phase 4 (MoE expert caching — the one access pattern the kernel can't predict) promoted to next; phase 2 (GPU tier) unaffected by the null result.**
  - **Phase 4 feasibility measured (same day)**: GLM MoE CPU 10.87 t/s uncapped → **1.12 t/s @ 10 GB cap** (10x collapse = the prize is real) — but per-selection `madvise(WILLNEED)` steering made it **60x worse** (0.018 t/s): WILLNEED blocks in direct reclaim inside a cgroup at its limit, ~1,100 calls/token. Page-cache hinting is dead for this tier in BOTH forms (dense: no win; MoE: catastrophic). **The viable phase-4/2 design is a userspace expert cache — pread/O_DIRECT into an owned (pinned) arena with explicit SLRU — which is the same machinery for CPU and GPU tiers; they merge into one build.** All measured evidence + the steering code preserved (env-gated) in common/ssd-streaming.cpp and the plan doc.
  - **Phase 2/4 feasibility spike — MEASURED PASS, GO (2026-07-08):** the design's one make-or-break number is random small-block O_DIRECT read bandwidth (the cache-miss path). Measured on this NVMe against the real 81 GB `DeepSeek-V4-Flash-IQ2XXS` (`scripts/ssd-stream-bench-odirect.cpp`): random 2.16 MB O_DIRECT reads = 1672 MB/s (1 thread) → **~2700 MB/s (2+ threads)**; 7.08 MB sets = 2448 → **~2800 MB/s**. Random reads *exceed* the 1.6 GB/s sequential baseline and O_DIRECT never touches the page cache (so the WILLNEED direct-reclaim −60x trap cannot recur). Model layout (`scripts/ssd-stream-gguf-layout.py`): deepseek4, 43 layers × 256 experts, 6 used + 1 shared/token; **77.9 GB of 81 GB is experts** (gate/up IQ2_XXS ~2.16 MB/slice, down Q2_K ~2.75 MB/slice), non-expert only **8.8 GB** (fits RAM or 1 GPU). Cold expert traffic = 258 slice-sets × 7.08 MB = **1.83 GB/token**. Projected IO-bound decode @2.7 GB/s: 0% cache **1.48 t/s** (already beats the 1.12 mmap-thrash floor by using pread not faults) · 44% (34 GB RAM cache) **2.64** · 80% skew **7.39** · 95% **29.6** (→ compute-bound). **Verdict: build the userspace O_DIRECT pread + SLRU expert cache.** The 81 GB DeepSeek (fits neither VRAM nor RAM) is the reference vehicle. Next: implement the streamed buffer type (below), then measure real hit rate + compute-bound ceiling.
  - **Design sketch (to validate in phase 0):** a *streamed* backend buffer type whose tensors are backed by `(file, offset)` instead of resident memory; a pinned host staging ring sized by `--ssd-stream-budget`; prefetch driven by graph execution order — double-buffer layer n+1's tensors while layer n computes (the graph-plan/scheduler hook point is the same place the decode-graph cache lives, so per-layer prefetch lists can be precomputed per cached graph shape); reads via O_DIRECT (+ io_uring) so streaming does not evict the resident CPU tier from page cache; no eviction complexity (weights are immutable, read-only); dequant stays on GPU exactly as today. GGUF is already offset-addressable per tensor — no file-format work needed.
  - **Phased plan:** 0 = investigation + baselines + plan doc (above); 1 = CPU-tier managed streaming (prefetch thread, staging ring), gate: byte-identical output + beats mmap-thrash tg measurably; 2 = GPU-tier streaming (staging ring → async H2D on a side stream overlapped with layer compute); 3 = placement policy + CLI + docs + compose profile; 4 = MoE-aware expert streaming with hot-expert cache; 5 (stretch) = cuFile/GDS direct SSD→VRAM, and interplay with distributed (task 12): worker-side streaming would let a TP island serve models bigger than the island's combined VRAM.
  - **Verification (house rules):** temp-0 byte-exactness vs fully-resident runs at every phase; enforced ceilings (RSS + VRAM measured under load); a tg/pp table per tier mix in docs/perf-tuning-v100.md.
- [x] **16. Meta shadow-arena abort on large graphs** — ✅ fixed (`4c837a751`, 2026-07-08). Production crash: `GGML_ASSERT(obj_new)` / "not enough space in the context's memory pool", 100% reproducible with two requests on Qwen3.6-35B-A3B (MTP, slot reuse) and at the end of long prefills on the 27B. Root cause: compute-ring shadow containers size their arenas as `compute_headroom` (16) views per static tensor; DeltaNet/hybrid graphs create more recurrent-state views per graph than that (27B: 64 layers → 2048 slots, 35B: 40 → 1280 — exactly the crashing pool sizes; both overflowed by exactly one `ggml_tensor_overhead()`). Fix at the mechanism: arenas grow on demand (full arena retired with its shadows alive, fresh same-size arena chained; ring rotation frees the chain) instead of a bigger constant that the next model shape would hit. Verified: the exact 35B two-request repro completes (+ a third reuse, zero warnings); full 27B in-process MTP gate byte-identical (63.6 t/s). Every image up to `v100-bigbro` carries the bug; fixed from `v100-bigbro2`. Related latent (not this crash, noted for upstream): `ggml-backend.cpp:1774` sizes the sched context with `sizeof(struct ggml_tensor)` instead of `ggml_tensor_overhead()`, and the `×2` per-input factor understates `n_copies>1` pipeline configs.
- [x] **17. Overlap AllReduce with the next subgraph's independent prefix** — ❌ **NEGATIVE RESULT (2026-07-08): implemented, correct, measured a LOSS — reverted. Do not re-attempt graph-level reduce/compute overlap.** Motivation held up: step-0 timing (`GGML_META_TIMING=1`, landed `6a5d2920d`/`bb1b7f2c1`) measured the tax at compute 60.3 ms + reduce 9.9 ms per 27B decode graph over 128 boundaries (~77 µs/boundary, ~14-15% of step wall). Full implementation was built and gauntlet-tested (`GGML_META_OVERLAP=1`: side backend instance + event pair per device; planning-time detection of each post-reduce subgraph's independent leading prefix via view-root aliasing; prefix submitted on the side stream behind ev_main while the AllReduce runs on the main stream, main re-joins via ev_side; per-device positional `ggml_graph_view` split — sound because device subgraphs are built 1:1 with meta node indices, ggml-backend-meta.cpp:2480).
  **Why it fails, both regimes (measured 2026-07-08, 2×V100, idle GPUs, temp-0 256-tok, `GGML_CUDA_ALLREDUCE=p2p`):**
  - **Dense/hybrid (Qwen 27B): the premise is false.** Diagnostic sweep: ALL 384 reduce boundaries have the reduced tensor consumed by the IMMEDIATELY next node (`attn_output→attn_residual` ADD, `ffn_out→l_out` ADD, `linear_attn_out→its own reshape`) — the residual stream makes everything downstream transitively dependent on every reduce. Zero hoistable prefix exists; the machinery is inert. 49.2 vs 49.3 t/s (noise), byte-identical.
  - **MoE (GLM 23B): prefixes exist but overlap LOSES 47%.** 138 boundaries/graph carry a real 4-node independent prefix (next layer's `ffn_gate` MUL_MAT is independent of the previous expert-sum reduce) — path fires, output byte-identical (twice), but tg **83.0 → 44.1 t/s**: +10.6 ms/token ≈ 77 µs/boundary of event-record/wait + doubled launch-call orchestration on Volta (no CUDA graphs → every extra launch/event is real CPU cost), which exceeds the entire reduce being hidden while a 4/71-node prefix hides almost nothing.
  **Conclusion:** the ~10 ms/token reduce tax is real but unrecoverable by scheduler-level reordering — where the graph is serial there is nothing to overlap, and where something exists it is too small to pay for the synchronization. The only viable successor is fusing the reduce into the producing GEMM (Megatron/TE-style chunked mul_mat + reduce-scatter-per-chunk epilogue) — a CUDA-kernel project in the mul_mat path, high effort, uncertain on Volta. Code reverted from the tree (this record + the git log of this entry preserve the design); `GGML_META_TIMING` diagnostic remains landed.
- [x] **18. MMVQ kernel tuning for Volta (sm70)** — ✅ **PARTIAL WIN landed (2026-07-08): +1.8% batch-1 K-quant decode, ppl-identical. Prod (MTP spec) neutral.** Root cause confirmed: Volta (cc 7.0) fell through to `MMVQ_PARAMETERS_GENERIC` in `mmvq.cu`, which lumps `ncols_dst` 1-4 at nwarps=4 — untuned for sm70. Added a dedicated `MMVQ_PARAMETERS_VOLTA` table_id (device + host `get_device_table_id`, `calc_nwarps`, `calc_rows_per_block`), differing from GENERIC only at the batch-1 decode path (`ncols_dst==1`): K-quants (Q2_K…Q6_K) now use **nwarps=2** (mirrors TURING, the closest tuned arch). **Measured sweep (single-GPU tg128, Qwen 27B Q4_K):** nwarps 1=30.21, **2=31.62**, 4(old)=31.06, 8=27.75 → 2 wins, **+1.8%**. **Correctness:** nwarps changes the cross-warp dot-product reduction order (not byte-exact by construction), gated on ppl instead — **PPL 5.9063 ± 0.14168 identical to 4 decimals** vs baseline (40 wikitext chunks), output coherent. Pascal/older keep GENERIC (unaffected). **Prod impact caveat:** the flagship MTP-spec config is NEUTRAL (70.42 vs 70.41 t/s) because MTP verify decodes at batch-4 (`ncols_dst=4`), which this change doesn't touch; the win applies to batch-1 nospec decode (single-stream, `docker-compose.nospec.yml`, low-acceptance long-context tail). **Investigated but NOT shipped:** tuning `ncols_dst` 2-4 (nwarps 1/2) gave +3-4% on *pure* single-GPU batched decode (llama-batched-bench npl=4) but was prod-NEUTRAL/-0.6% on the real 2-GPU MTP-spec path (AllReduce + draft overhead dominate, not the half-width matvec) and perturbed verify numerics (draft counters 1874→1909) — reverted. Follow-up: revisit `ncols_dst` 2-4 specifically for the nospec `-np>1` aggregate profile (89.6 t/s baseline) where the small-batch matvec is a larger fraction. Harness: `llama-bench`/`llama-batched-bench`, `scratchpad/spec_bench.py`. Task 19 (FA depth decay) is the remaining same-class lever.
- [x] **19. Flash-attention long-context decay on Volta** — ✅ **CLOSED (2026-07-08, `723cf9fed`): SIZED + bottleneck identified; easy levers exhausted; deep kernel work deferred.** Prize measured (single-GPU tg, Qwen 27B, `llama-bench -d`): 31.5 (d0) → 26.2 (d32k) → 22.1 (d65k) t/s = **−30%**, i.e. ~13.5 ms/token of FA at 65k (only the full-attention layers decay; DeltaNet is depth-independent). **Decode kernel confirmed** (instrumented `ggml_cuda_get_best_fattn_kernel`): `BEST_FATTN_KERNEL_VEC` = `fattn-vec.cuh`, D=256, GQA≤2, stream-K KV-split already engaged on Volta (`fattn-common.cuh:1126` — `tiles_efficiency_percent < 75` true at batch-1). **Bottleneck class = COMPUTE/instruction-bound, NOT memory-bound** — the decisive test: quantizing KV *worsens* the depth penalty instead of halving it (d0→d65k decay: f16 −29.8%, q8_0 −32.0%, q4_0 −34.6%; Δms/tok @65k: f16 13.5, q8_0 15.2, q4_0 17.3). **→ Actionable negative: do NOT use quantized KV for long-context *speed* on Volta (it adds dequant compute to an already compute-bound kernel; quant KV is still valid purely for *capacity*).** **Easy launch-config lever exhausted:** the vec kernel hardcodes `nthreads=128` for all archs (`fattn-vec.cuh:4-11`); swept for Volta → d65k: 64=21.8, **128=22.1 (best)**, 256=21.4 — already optimal, no win (unlike task 18's nwavps knob). Remaining headroom needs genuine sm70 kernel-algorithm work (KQ dot-product instruction scheduling / dp4a use / online-softmax overhead in `fattn-vec.cuh` + `fattn-common.cuh` vec_dot) — the "high effort/risk" path, uncertain payoff, deferred to a focused kernel session. ncu profiling blocked in-container (`ERR_NVGPUCTRPERM`; would need host `NVreg_RestrictProfilingToAdminUsers=0`).
- [x] **20. Adaptive speculative draft cap** — ✅ **MEASURED (2026-07-08): tg-NEUTRAL, does not win. Keep OFF (default), do not enable in prod.** Mechanism (landed `64788b8a8`, env-gated `LLAMA_SPEC_ADAPTIVE=1`): acceptance EMA (warm-started at n_max) caps each draft round at EMA+1 within [n_min, n_max] across all three draft loops — saves draft passes on cold content; verify batch shape unaffected (drafts pad to n_max), target temp-0 output unchanged by construction (sampling policy only). **Bench (27B MTP, `-sm tensor -ts .5,.5 -np 1 -c 16384`, p-min 0.75 n-max 3, 8 mixed prompts × 256 tok, idle 2×V100):** adaptive deterministically cut draft tokens generated 1874 → 1779 (**−5%**) but aggregate tg **70.4 → 70.6 t/s (+0.3%, inside the ~0.1 t/s run-to-run noise)** and accepted tokens dropped 1303 → 1284. Confirms the honest expectation: the p_min confidence gate already captures the early-exit value, and MTP drafting is cheap relative to verify, so cutting draft passes doesn't move tg. Code stays landed (env-gated off) as a documented no-op; the `LLAMA_SPEC_ADAPTIVE=1` line added to `docker-compose.mtp.yml` was reverted (no benefit). Harness: `scratchpad/spec_bench.py` (server `draft_n`/`draft_n_accepted` timings). Next: kernel-level levers (tasks 18/19) are the only remaining tg headroom.
- [x] **21. Refresh the README benchmark numbers with real, reproducible measurements** — 🔄 **SINGLE-STREAM DONE (2026-07-09); 4-concurrent aggregate remaining.** Replaced the stale "Headline results" table with freshly-measured **per-model single-stream tables** (Qwen3.6-27B dense, Qwen3.6-35B-A3B MoE, GLM-4.7-Flash-UD) across 8 GPU/CPU/MTP configs, 2× V100-32GB NVLink, temp-0, image from this tree. **Key methodology finding: `llama-cli` short-`-n` runs UNDERCOUNT MTP** — speculation has a warmup ramp (draft-acceptance EMA + graph cache) that only settles after ~500 tokens, so the ¹-marked MTP numbers were re-measured from the **production server** (`docker-compose.mtp.yml`) via `docker logs` steady-state `tg`/`tg_3s` (27B tensor+MTP ~81, 35B ~166, 35B 1-GPU ~140, 27B 1-GPU ~51, 35B CPU ~14; ~88.6% draft acceptance). Non-MTP validated cli==server. README reproduction section now uses the compose (server), not cli. **4-concurrent aggregate measured (2026-07-09): 35B A3B `-np 4` = ~263 t/s aggregate** (4 simultaneous code-gen requests, 24,238 completion tokens / 92 s wall; per-slot 68.6-69.9 t/s each — every stream faster than the 27B's single-stream peak; draft acceptance 86-89%; ~277 t/s during full 4-way concurrency before the tail). The old unverified "125.8" (27B) headline no longer appears anywhere in the README — every number in it is now freshly measured. **Residual (minor):** `docs/perf-tuning-v100.md` still carries historical per-change numbers from older builds — fine as a historical record, but a note pointing to the README tables as the current reference would help.
  - **Start small to get real numbers now, cheaply**: both small models are already on-box — `Qwen3-0.6B-BF16.gguf` (1.2 GB, fastest iteration) and `Qwen3.5-9B-UD-Q4_K_XL.gguf` (6.1 GB, more representative). Use one of these to (a) build/settle the bench harness and table format, (b) publish a first honest row or two, and (c) prove the numbers are reproducible run-to-run.
  - **Later** (separate sitting, needs a quiet 2-GPU window): extend the same harness to the headline models — `Qwen3.6-27B-UD-Q4_K_XL-MTP` (prod MTP-spec, `docker-compose.mtp.yml`), `Qwen3.6-35B-A3B` (MoE), GLM-4.7-Flash (single-GPU MoE), and DeepSeek-V4-Flash 81 GB (ssd-streaming) — and refresh the full README table + `docs/perf-tuning-v100.md`.
  - Harness candidates already in-tree: `llama-bench` / `llama-batched-bench` (pp/tg sweeps), `scratchpad/spec_bench.py` (server-side MTP draft/accept timings). Keep it a script so re-running after future changes is one command. Note the "Before" column is a historical baseline (`d2b706d1b`) — decide whether to keep it or drop to a single current-numbers table.
