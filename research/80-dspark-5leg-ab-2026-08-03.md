# #80 proper 5-leg A/B — EP/layer x dspark/target-only + block-5 (2026-08-03)

Vehicle: DeepSeek-V4-Flash-0731 UD-Q8_K_XL (161.87 GB decimal / 150.75 GiB), fleet =
CUDA0 + CUDA1 (V100-32G x2) + .11:50052 + local:50053 + .15:50055 (all proto 4.15,
image a8472e5bd; launcher aac619b09 client = has the #12 ctx_other mirror fix).
Protocol: scripts/measure-fleet-leg.sh (100-token greedy, cache_prompt false), RUNS=8,
2 warmups after load, measured immediately post-load (cold-vs-cold across legs; the
#104 idle-drop trap makes prompt-after-load mandatory). Coherence gate per leg = the
train-distance chat probe (reasoning model: judged on reasoning_content + content).

## Leg definitions

| Leg | Split | Spec | Shape |
|---|---|---|---|
| L1 | EP (-sm tensor, ATTN_OWNER=0,1) | dspark n3 conf0.3 pinned CUDA1 | -ts 12,6,17,35,30 (CUDA0,CUDA1,.11,local,.15) |
| L2 | EP same | none | same |
| L3 | layer | none | -ts 12,15,30,26,17 (CUDA0,CUDA1,.11,local,.15) - the "4.7 keeper" shape |
| L4 | layer, workers-first device order | dspark n3 conf0.3 pinned CUDA1 | -ts 30,30,17,12,11 (.11,local,.15,CUDA0,CUDA1) |
| L5 | EP same as L1 | dspark n5 conf0.3 | same as L1 |

EP legs env: EP_ONLY=1 ATTN_OWNER=0,1 KV_RESERVE_MB=6144 BCAST_FUSE=2 WIRE_F16=1
WIRE_Q8=1 EXPERT_DEFER=1 (byte-identical across EP legs; NOTE: BOUNDARY_STATS not set,
so META_EXPERT_DEFER counters do not print - keep env identical rather than perturb
mid-A/B; quality gate = coherence reads). Layer legs env: none (wizard layer template).

## Results

### L1 EP + dspark n3 — mean 3.37 t/s, acceptance 45-68%, coherence PASS

Runs: 3.61, 3.65, 3.28, 2.72, 3.12, 3.86, 3.29, 3.46 (t/s). Draft acceptance per
print_timing: 0.447-0.681, mean len 2.12-2.82. Coherence: train probe correct (90 km,
clean reasoning). Ops note: first load attempt hit the #104 idle-drop chain (all 3
worker sockets dropped at first decode after the 1-h load; local worker restart x2 via
the remote-shutdown recovery lane; full --rpc-reload; second attempt measured clean).
The 2026-08-02 "5.06 t/s / 85.2%" was a smoke read on a friendlier prompt class - the
acceptance gap (85% vs ~50%) is prompt-mix, not a regression; this protocol's numbers
only compare within this table.

### L2 EP target-only — mean 4.19 t/s, coherence PASS

Runs: 3.91, 4.27, 4.20, 4.25, 4.29, 4.29, 4.37, 3.93. Train probe correct. Clean load,
no #104 recurrence (measured promptly post-load). **EARLY SIGNAL: EP target-only 4.19
BEATS EP+dspark 3.37 on the standard prompt - dspark is below break-even on the EP
fleet shape at ~50% prose acceptance** (the union-multiplier law holds; the 85%/5.06
smoke was a high-acceptance prompt class).
### L3 layer target-only — mean 2.01 t/s (dead flat 2.01-2.03), coherence PASS, zero drops

**Baseline reinterpretation:** the ledger's "4.7 t/s layer target-only keeper" was NOT
layer split. 2.01 matches the wizard's own fleet-layer estimate (~1.5-2.5) and the
sum-of-stages law; the user's saved "EP 3x Fleet low profile" wizard config carries
exactly the keeper shares (12/15/30/17/26) - **the 2026-08-01 4.7 serve was EP
target-only.** The EP-vs-layer target-only gap is 2.1x (4.19 vs 2.01) on this
protocol.
### L4 layer + dspark — pending

### L5 EP + dspark n5 — mean 2.70 t/s (7 runs), acceptance 33-55%, coherence PASS (run-1 read)

Runs: 2.58, 2.68, 2.48, 2.89, 2.72, 2.54, 3.00, [run 8 = Compute error, #104 drop].
Mean accepted len 1.60-3.04 at n_max 5 (vs 2.12-2.82 at n3) - **the accepted chain
saturates around ~2-3 tokens; block-5 verify pays 5-lane union reads for ~2.5-token
chains -> n5 (2.70) < n3 (3.37) < no-spec (4.19). Block-verify economics on this
fleet: NEGATIVE, monotonically worse with depth.** (Chat coherence probe was lost to
the run-8 recovery window; run-1 completion text read coherent.)

## #104 recurrence color (3 events this session)

Every event: '10.5.5.11:50052' fails FIRST (boundary_fused_recv once, buffer_free_buffer
twice), then local:50053 and .15:50055 report failed within ~1.5 ms - the same-ms
cascade is the coordinator's endpoint-failure handler tearing down the set, NOT three
independent worker deaths. .11's link is the real dropper, ~10-40 min after connect,
active traffic no protection. .11 conntrack tcp_timeout_established = 432000 (5 days,
not the mechanism); ufw active on .11; workers themselves stay up (local restarts were
the recovery lane's remote-shutdown, exit 0). Client-side TCP keepalive (#104 fix)
remains the answer; until then: measure immediately post-load and expect one recovery
per ~30 min of serving.

## Verdict — pending all legs
